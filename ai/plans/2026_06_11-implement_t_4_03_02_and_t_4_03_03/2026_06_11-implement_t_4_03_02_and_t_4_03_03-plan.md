# Plan: Implement T-4.03-02 and T-4.03-03

## Goal

Implement the GoogleSheetsAdapter to satisfy the extended SpreadsheetPort for Google Sheets API v4, and implement the DrizzleSpreadsheetConfigRepository to persist spreadsheet configuration records. Both are Infrastructure-layer deliverables for HU-4.03 sheet selection.

## Context

- `src/domain/ports/services.ts` - Domain contract `SpreadsheetPort` already declares `listSheets` and `getHeaders`.
- `src/domain/entities/SheetInfo.ts` - Immutable value object for sheet metadata.
- `src/domain/ports/repositories.ts` - `ISpreadsheetConfigRepository` interface with `findByUserId`, `create`, and `updateAccessVerified`.
- `src/infrastructure/db/schema/index.ts` - `spreadsheetConfigs` table is already defined (line 150).
- `src/infrastructure/adapters/drive/GoogleDriveFileDiscoveryAdapter.ts` - Reference pattern for fetch-based Google API adapters (Bearer auth, structured logging, error mapping).
- `src/infrastructure/db/repositories/DrizzleOAuthTokenRepository.ts` - Reference pattern for Drizzle repositories (mappers, `.returning()`, throws on empty result).
- `src/infrastructure/db/repositories/DrizzleConversationStateRepository.spec.ts` - Reference pattern for mocked Drizzle database tests.
- `docs/plans/plan-conventions.md` - Plan structure conventions.
- `docs/architecture/data-model.md` - Must be updated to reflect the Option A convention for `accessVerifiedAt`.

## Phases

### Phase 1: GoogleSheetsAdapter

Implement the `GoogleSheetsAdapter` in `src/infrastructure/adapters/sheets/` with `listSheets` and `getHeaders` methods. Use direct `fetch` calls to Google Sheets API v4. Add error classes following the existing `FileDiscoveryError` pattern.

- [x] Create `src/infrastructure/adapters/sheets/GoogleSheetsAdapter.ts` implementing `SpreadsheetPort`.
- [x] Implement `listSheets(fileId)`:
  - Call `GET https://sheets.googleapis.com/v4/spreadsheets/{fileId}?fields=sheets.properties(title,index)`.
  - Map response to `SheetInfo[]` using `new SheetInfo({ name, index })`.
  - Handle network errors, non-2xx HTTP, and invalid JSON with structured `console.error` logging.
- [x] Implement `getHeaders(fileId, sheetName)`:
  - Call `GET https://sheets.googleapis.com/v4/spreadsheets/{fileId}/values/{sheetName}!1:1`.
  - Return `string[]` from the first row of values.
  - Handle network errors, non-2xx HTTP, and invalid JSON with structured `console.error` logging.
- [x] Create `src/domain/errors/SpreadsheetError.ts` following the `FileDiscoveryError` pattern: extends `Error`, descriptive message, used for adapter failures.
- [x] Create `src/infrastructure/adapters/sheets/GoogleSheetsAdapter.spec.ts` with mocked `fetch` tests:
  - `listSheets` returns `SheetInfo[]` on valid response.
  - `listSheets` throws `SpreadsheetError` on network/HTTP/JSON errors.
  - `getHeaders` returns `string[]` on valid response.
  - `getHeaders` throws `SpreadsheetError` on network/HTTP/JSON errors.
  - Unimplemented methods throw `SpreadsheetError`.
- [x] Run `pnpm run lint` and `pnpm run typecheck` to verify linting and typechecking. Fix issues if any.
- [x] Ask the user if they want to review the changes before continuing, or proceed directly with the next phase.

### Phase 2: DrizzleSpreadsheetConfigRepository

Implement the `ISpreadsheetConfigRepository` interface using Drizzle ORM. Create unit tests with a mocked database, generate the initial Drizzle migration, and update the data model documentation.

- [x] Create `src/infrastructure/db/repositories/DrizzleSpreadsheetConfigRepository.ts` implementing `ISpreadsheetConfigRepository`.
- [x] Implement `findByUserId(userId)`:
  - Query `spreadsheetConfigs` by `userId`.
  - Return mapped `SpreadsheetConfig` or `null`.
- [x] Implement `create(config)`:
  - Insert `userId`, `provider`, `fileId`, `fileName`, `sheetName`, `accessVerifiedAt` (placeholder `new Date()`).
  - Return mapped `SpreadsheetConfig`.
- [x] Implement `updateAccessVerified(id)`:
  - Update `access_verified_at` to `new Date()` for the given `id`.
- [x] Create `src/infrastructure/db/repositories/DrizzleSpreadsheetConfigRepository.spec.ts` with mocked `PostgresJsDatabase` tests:
  - `findByUserId` returns mapped entity or `null`.
  - `create` inserts and returns mapped entity with `accessVerifiedAt`.
  - `updateAccessVerified` updates timestamp.
  - All methods throw on empty DB result.
- [ ] Run `pnpm db:generate` to generate the initial Drizzle migration (all schema tables). *(User will run manually)*
- [x] Update `docs/architecture/data-model.md` to document the Option A convention: `accessVerifiedAt` is initialized to a placeholder timestamp on first creation and updated to the real timestamp during HU-4.04.
- [x] Run `pnpm run lint` and `pnpm run typecheck` to verify linting and typechecking. Fix issues if any.
- [x] Ask the user if they want to review the changes before continuing, or proceed directly with the next phase.

## Next step

All phases are complete. Run `pnpm db:generate` to generate the Drizzle migration (user will run manually). Then proceed with the next task: T-4.03-04 Implement HandleSheetSelection use case.
