### Task T-4.04-06: QA scenarios and feature documentation update

- **Description:** Validate all four acceptance scenarios end-to-end, including a simulated expired token test. Review the implementation for Clean Architecture boundary compliance (no business logic in handler, use case is provider-agnostic). Update the feature documentation (`docs/features/`) to reflect the implemented behavior. Ensure `pnpm lint && pnpm typecheck && pnpm test` all pass green.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Scenario 1 (success): verified — transparent flow, no user message.
  - [x] Scenario 2 (read-only): verified — correct message and instructions shown.
  - [x] Scenario 3 (empty sheet): verified — confirmation prompt shown.
  - [x] Scenario 4 (access error with retry): verified — retry occurs, persistent error triggers reconnection suggestion.
  - [x] Simulated expired token test passes.
  - [x] `pnpm lint && pnpm typecheck && pnpm test` all pass.
  - [x] Feature documentation updated under `docs/features/`.
  - [x] Clean Architecture boundary is enforced: no business logic in handler.

- **Technical dependencies (block the start):** T-4.04-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** QA / Backend

- **Technical notes:** Run `pnpm lint && pnpm typecheck && pnpm test` as the ship check per `AGENTS.md` Done Gates. Update `docs/features/` per Documentation Sync rules. If the feature doc file doesn't exist yet, create it following the project's feature doc conventions.
