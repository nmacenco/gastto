### Task T-4.01-09: Integrate connection flow into message worker

- **Description:** Replace the onboarding placeholder in `src/interfaces/workers/message.worker.ts` with real handlers for `ONBOARDING_START` and `ONBOARDING_DRIVE`. Wire the new Application use cases into `MessageWorkerDeps` and `src/main.ts`.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] `MessageWorkerDeps` extended with `initiateConnection`, `handleCallback`, `cancelConnection`, `sendReminder` (or a combined interface)
  - [x] `ONBOARDING_START` handler presents the two provider options and parses the user's choice
    - [x] Valid choice → calls `InitiateCloudConnectionUseCase` → transitions to `ONBOARDING_DRIVE`
    - [x] Invalid choice → re-prompts without state change
  - [x] `ONBOARDING_DRIVE` handler checks for "cancelar" keyword
    - [x] Matches → calls `CancelCloudConnectionUseCase` → transitions back to `ONBOARDING_START`
    - [x] No match → sends "Please complete the authorization in your browser or type cancel to abort"
  - [x] `src/main.ts` injects the new use cases into `createMessageWorker`
  - [x] Clean Architecture boundary explicitly maintained: worker only deserializes raw message, validates, and delegates; all business logic lives in Application use cases
  - [x] `MessagingOutputPort` (not `IChatMessenger`) is used for sending messages from the worker, keeping the channel agnostic

- **Technical dependencies (block the start):** T-4.01-05, T-4.01-08
- **Delivery dependencies (block the merge):** T-4.01-10

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** The existing placeholder is lines 77-89 in `message.worker.ts`. `MessagingOutputPort.sendMessage` is sufficient for plain-text prompts. Keep WhatsApp compatibility by relying only on the port abstraction (no Telegram-specific APIs in the worker).
