### Task T-4.01-02: Implement DrizzleOAuthTokenRepository

- **Description:** Implement the existing `IOAuthTokenRepository` port using Drizzle ORM. The repository must encrypt and decrypt tokens using AES-256-GCM per ADR-007 before persistence and after retrieval.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [ ] `DrizzleOAuthTokenRepository` created in `src/infrastructure/db/repositories/DrizzleOAuthTokenRepository.ts`
  - [ ] Implements all `IOAuthTokenRepository` methods: `findByUserAndProvider`, `upsert`, `markRefreshed`, `markRevoked`
  - [ ] Uses the project's encryption utility (AES-256-GCM) for `accessTokenEnc` and `refreshTokenEnc`
  - [ ] Unit tests with test DB pass (`pnpm test`)
  - [ ] Migration already exists (`oauth_tokens` table); no new migration required for this task

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.01-06

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Refer to `DrizzleConversationStateRepository.ts` for the repository pattern. The `oauth_tokens` schema is in `src/infrastructure/db/schema/index.ts`. Encryption key comes from `ENCRYPTION_KEY` env var (ADR-007).
