### Task T-4.01-07: Implement cancel and reminder use cases

- **Description:** Implement two small Application use cases: one to cancel an in-progress OAuth flow when the user types "cancelar", and one to send the 10-minute reminder if the user hasn't completed authorization.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] `CancelCloudConnectionUseCase` removes OAuth state from Redis and cancels the pending reminder BullMQ job
  - [ ] `SendOAuthReminderUseCase` checks if tokens exist; if not, rebuilds the auth URL with a fresh state, re-schedules a reminder, and sends the link again
  - [ ] Both return DTOs with appropriate messages and next states
  - [ ] Unit tests pass

- **Technical dependencies (block the start):** T-4.01-05
- **Delivery dependencies (block the merge):** T-4.01-08

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Store the BullMQ job ID in Redis alongside the OAuth state so cancellation is possible. Reminder re-scheduling must clean up the old state to avoid duplicates.
