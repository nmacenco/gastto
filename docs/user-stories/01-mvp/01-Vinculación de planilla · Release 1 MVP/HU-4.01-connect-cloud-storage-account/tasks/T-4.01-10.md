### Task T-4.01-10: Write tests and feature documentation

- **Description:** Add meaningful tests for the new use cases and routes, and create the canonical feature documentation.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Unit tests for `InitiateCloudConnectionUseCase`, `HandleOAuthCallbackUseCase`, `CancelCloudConnectionUseCase`, `SendOAuthReminderUseCase` with mocked ports
  - [x] Integration tests for `GoogleDriveOAuthAdapter` and `OneDriveOAuthAdapter` mocking external HTTP calls (MSW or `vi.fn()`)
  - [x] Integration tests for `DrizzleOAuthTokenRepository` using test database
  - [x] Fastify route tests for `/auth/google/callback` and `/auth/microsoft/callback`
  - [x] `docs/features/cloud-storage-connection.md` created with:
    - [x] Flow description and sequence diagram
    - [x] API contracts (route schemas, DTOs)
    - [x] Error cases and retry behavior
    - [x] QA checklist for both providers
  - [x] `pnpm lint && pnpm typecheck && pnpm test` pass green

- **Technical dependencies (block the start):** T-4.01-08, T-4.01-09
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Mock only at boundaries (network, filesystem, DB). Never mock core business logic. Use the existing `*.spec.ts` patterns (Vitest). Follow `docs/testing/guidelines.md` for coverage targets.
