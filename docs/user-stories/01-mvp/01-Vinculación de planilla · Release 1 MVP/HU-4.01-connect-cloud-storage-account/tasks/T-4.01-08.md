### Task T-4.01-08: Implement Fastify OAuth callback routes and reminder worker

- **Description:** Register Fastify routes to receive OAuth callbacks from Google and Microsoft browsers. Create the BullMQ worker that processes delayed reminder jobs and wire both into `src/main.ts`.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] Fastify route `GET /auth/google/callback` registered with Zod query schema (`code`, `state`)
  - [x] Fastify route `GET /auth/microsoft/callback` registered with Zod query schema (`code`, `state`)
  - [x] Route handlers deserialize params, validate with Zod, and delegate to `HandleOAuthCallbackUseCase`
  - [x] Routes return a simple HTML response ("You can close this window") or redirect
  - [x] BullMQ worker `oauth-reminder` created in `src/interfaces/workers/oauthReminder.worker.ts`
  - [x] Worker registered in `src/main.ts` with Redis connection
  - [x] Worker queue registered in `src/main.ts` so use cases can add delayed jobs
  - [x] Route tests instantiate Fastify independently and register validator/serializer compilers from `fastify-type-provider-zod`

- **Technical dependencies (block the start):** T-4.01-06, T-4.01-07
- **Delivery dependencies (block the merge):** T-4.01-09

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the existing `telegram.webhook.ts` pattern for route registration. Ensure `app.withTypeProvider<ZodTypeProvider>()` is used. Swagger schema must include `tags`, `description`, and `response` schemas per AGENTS.md.
