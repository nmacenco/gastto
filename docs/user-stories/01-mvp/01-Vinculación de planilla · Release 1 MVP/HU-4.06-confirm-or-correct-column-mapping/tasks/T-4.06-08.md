### Task T-4.06-08: Add unit tests for use cases and validation logic

- **Description:** Write Vitest tests for the confirm use case, correct-field use case, and natural-language parser. Mock only external boundaries (repositories, spreadsheet port). Ensure coverage for single-field correction, multi-field cumulative correction, invalid column handling, and confirmation flow.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Tests exist for the confirm mapping use case.
  - [x] Tests exist for the correct field use case, including invalid-column branch.
  - [x] Tests exist for the natural-language parser with multiple phrasings.
  - [x] No core business logic is mocked.
  - [x] All tests pass with `pnpm test`.

- **Technical dependencies (block the start):** T-4.06-03, T-4.06-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** QA / Backend

- **Technical notes:** Follow `docs/testing/guidelines.md`. Use Vitest mocks at repository/port boundaries. Test data can be built with factory functions or simple objects.
