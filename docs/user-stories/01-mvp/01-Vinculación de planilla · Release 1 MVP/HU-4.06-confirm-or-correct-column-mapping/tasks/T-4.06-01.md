### Task T-4.06-01: Define domain model for column mapping confirmation and correction state

- **Description:** Create or refine domain value objects/entities that represent a confirmed column mapping and the transient correction state. Include the fields exposed by HU-4.05 (e.g., date, amount, description, category, payment method) and a state marker indicating whether the mapping is being confirmed, corrected, or finalized. Keep the model immutable and free of infrastructure concerns.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [x] Value object/entity exists for `ColumnMapping` and `MappingCorrectionState`.
  - [x] Correction state tracks original proposed mapping and accumulated corrections.
  - [x] No dependency on Redis, Telegram, LLM, or spreadsheet adapters in the Domain layer.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.06-02, T-4.06-04

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Place under `src/domain/value-objects/` or `src/domain/entities/` following existing project conventions. Refer to HU-4.05 output for the structure of the proposed mapping.
