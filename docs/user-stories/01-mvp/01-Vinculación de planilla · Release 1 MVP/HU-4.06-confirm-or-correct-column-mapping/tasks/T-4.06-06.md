### Task T-4.06-06: Implement Redis conversation state for mapping correction flow

- **Description:** Implement an Infrastructure adapter that persists the mapping correction state in Redis with a 30-minute TTL. The adapter must implement the `ConversationStateRepository` application port and support resuming the flow from where the user left off.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [ ] Adapter implements `ConversationStateRepository`.
  - [ ] State is serialized/deserialized safely (JSON).
  - [ ] Keys expire after 30 minutes of inactivity.
  - [ ] On resume, the adapter can retrieve the pending state or report that the flow has expired.

- **Technical dependencies (block the start):** T-4.06-02
- **Delivery dependencies (block the merge):** T-4.06-07

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Use existing Redis helpers under `src/infrastructure/redis/`. Key naming convention: `conversation:{userId}:mapping-correction`. TTL is configurable via `src/config/`.
