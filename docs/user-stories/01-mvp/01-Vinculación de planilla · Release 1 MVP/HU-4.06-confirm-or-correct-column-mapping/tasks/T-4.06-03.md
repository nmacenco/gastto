### Task T-4.06-03: Implement confirm column mapping use case

- **Description:** Implement the application use case that finalizes the column mapping when the user confirms with "yes", "ok", or equivalent. The use case validates that a proposed mapping exists, marks it as confirmed, persists it, and returns the next step (category confirmation, HU-4.07).

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] Use case accepts a user/session identifier and a confirmation signal.
  - [x] It loads the proposed mapping from the repository.
  - [x] On success, it persists the confirmed mapping and clears the correction state.
  - [x] It returns a result indicating advancement to HU-4.07.

- **Technical dependencies (block the start):** T-4.06-02
- **Delivery dependencies (block the merge):** T-4.06-07

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** This use case must not know about Telegram or Redis. Use the logger injected via constructor DI (`src/infrastructure/logger.ts`) only from the caller, or keep the use case pure and let the interface layer log.
