### Task T-4.07-08: Write integration tests and QA coverage

- **Description:** Write integration and end-to-end tests that verify the full category confirmation flow across all layers. Tests must cover: populated spreadsheet with multiple categories, empty spreadsheet with defaults, adding a category via natural language, correcting a category name, and confirming without changes. Ensure that the Drizzle migration is applied to the test database and that the feature doc under `docs/features/` is created or updated.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Integration test covers the happy path: mapping confirmed → categories detected → user confirms → onboarding closed.
  - [ ] Integration test covers empty-column path with default categories.
  - [ ] Integration test covers adding and renaming categories via natural language.
  - [ ] `pnpm test` passes green; no mocks on core business logic (only on external Telegram/Sheets APIs).
  - [ ] Feature documentation is created/updated under `docs/features/category-confirmation.md`.
  - [ ] `docs/architecture/data-model.md` is updated with the new schema.

- **Technical dependencies (block the start):** T-4.07-07
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1.5 hours

- **Suggested role:** QA / Backend

- **Technical notes:** Use Vitest for all tests. Spin up a test PostgreSQL instance (or use `pg-mem` / `testcontainers`) so Drizzle migrations run against a real schema. Mock the Telegram API and Sheets adapter at the boundary, but exercise the Domain and Application layers through real objects. Run `pnpm lint && pnpm typecheck && pnpm test` before marking done, per the Ship Check in `AGENTS.md`.
