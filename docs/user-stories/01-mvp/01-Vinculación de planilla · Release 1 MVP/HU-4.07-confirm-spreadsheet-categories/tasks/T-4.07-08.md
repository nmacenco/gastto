### Task T-4.07-08: Implement Redis conversation state adapter for category confirmation flow

- **Description:** Implement the infrastructure adapter that persists the current `CategoryVocabulary` and conversation state (detecting, confirming, editing, confirmed) in Redis. The adapter implements `CategoryVocabularyRepositoryPort` and any onboarding state port required. It must serialize/deserialize safely, handle TTL so stale sessions expire, and recover gracefully when no state is found.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] Adapter implements `CategoryVocabularyRepositoryPort`.
  - [x] Saves and loads vocabulary + state keyed by user/session identifier.
  - [x] Uses TTL to avoid leaking orphan session data.
  - [x] Returns `null`/empty result when no state exists, allowing the use case to prompt recovery.
  - [x] Does not expose Redis-specific types to the application layer.

- **Technical dependencies (block the start):** T-4.07-02
- **Delivery dependencies (block the merge):** T-4.07-09

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Place under `src/infrastructure/redis/` or `src/infrastructure/db/repositories/` depending on project convention. Reuse the Redis connection helper from previous HUs. Keep serialization format versioned to ease future migrations.
