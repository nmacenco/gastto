### Task T-4.07-10: Add unit tests for category vocabulary use cases and parser

- **Description:** Write unit tests for the category detect/present use case, confirm use case, add/correct use case, and natural-language parser. Mock only external boundaries (spreadsheet reader, repository, onboarding port); never mock core business logic. Cover detected categories, empty-column defaults, add/remove/rename edits, duplicate handling, and missing-state recovery.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Tests exist for the natural-language parser covering add, remove, rename, and no-match cases.
  - [x] Tests exist for the detect/present use case covering detected values and empty-column defaults.
  - [x] Tests exist for the confirm use case covering confirmation and missing-state recovery.
  - [x] Tests exist for the add/correct use case covering add, remove, rename, duplicate, and not-found scenarios.
  - [x] All tests run with `pnpm test` and pass.
  - [x] Mocks are limited to ports/adapters, not domain rules.

- **Technical dependencies (block the start):** T-4.07-04, T-4.07-05, T-4.07-06, T-4.07-07
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** Backend / QA

- **Technical notes:** Use Vitest per `AGENTS.md`. Place tests alongside source files (`*.test.ts`) or under `tests/unit/` following the project convention. Include tests with accented characters and mixed-case category names.
