### Task T-4.07-03: Implement spreadsheet unique category values reader

- **Description:** Implement the infrastructure adapter that reads the mapped category column from the user's spreadsheet, filters out empty cells and duplicates (case-insensitive), normalizes whitespace, and returns the sorted unique values to the application layer. The adapter must receive the confirmed column mapping from the previous step (HU-4.06) and use the existing spreadsheet access mechanism.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] Adapter implements `CategoryReaderPort`.
  - [x] Returns unique, non-empty category values from the mapped column.
  - [x] Handles case-insensitive deduplication and whitespace normalization.
  - [x] Returns an empty list when the column has no values instead of failing.
  - [x] Logs errors structurally without leaking stack traces.

- **Technical dependencies (block the start):** T-4.07-02
- **Delivery dependencies (block the merge):** T-4.07-05

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Use the existing spreadsheet adapter from HU-4.04/HU-4.05. Place the implementation under `src/infrastructure/adapters/sheets/` or the equivalent Excel adapter. Refer to `AGENTS.md` for Pino logging conventions.
