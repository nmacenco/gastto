### Task T-4.07-02: Define application ports for category vocabulary persistence

- **Description:** Define the application-layer output ports required by the category confirmation flow: one to read unique category values from the spreadsheet category column, one to persist and retrieve the user's category vocabulary, and one to mark the onboarding as completed. Keep ports interface-only and free of infrastructure details.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] `CategoryReaderPort` exists with a method to read unique values from the mapped category column.
  - [x] `CategoryVocabularyRepositoryPort` exists for saving and loading the vocabulary.
  - [x] `OnboardingCompletionPort` exists for signaling onboarding completion (if not already defined by previous HUs).
  - [x] Ports depend only on domain/application types, not on external adapters.

- **Technical dependencies (block the start):** T-4.07-01
- **Delivery dependencies (block the merge):** T-4.07-03, T-4.07-05, T-4.07-06, T-4.07-07, T-4.07-08

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Place ports under `src/application/ports/` or `src/domain/ports/` following project conventions. Reuse existing ports from HU-4.05/HU-4.06 where they already cover spreadsheet reading or onboarding state.
