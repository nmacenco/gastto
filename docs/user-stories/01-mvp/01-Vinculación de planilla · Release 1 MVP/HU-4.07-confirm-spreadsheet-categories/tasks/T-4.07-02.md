### Task T-4.07-02: Define category vocabulary domain model

- **Description:** Create the domain entity and value objects that represent a user's category vocabulary. This includes the `Category` entity with an ID and name, and a `CategoryVocabulary` aggregate that holds the full set of categories for a user. Define any invariants (e.g., no duplicate names, max length) in the domain layer.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [ ] `Category` entity exists with id, name, and optional normalizedName.
  - [ ] `CategoryVocabulary` aggregate exists with methods to add, remove, and rename categories.
  - [ ] Domain invariants reject duplicate category names (case-insensitive).
  - [ ] Unit tests cover all aggregate operations and invariant violations.

- **Technical dependencies (block the start):** T-4.07-01
- **Delivery dependencies (block the merge):** T-4.07-03

- **Estimation:** 0.5 hours

- **Suggested role:** Backend

- **Technical notes:** Place files under `src/domain/entities/` and `src/domain/value-objects/`. Keep the model agnostic of persistence and external services. Use immutable value objects where possible. Follow existing entity patterns in the codebase.
