### Task T-4.02-04: Integrate file selection into message worker

- **Description:** Wire the `HandleSpreadsheetFileSelection` use case into the `MessageWorkerDeps` and replace the `ONBOARDING_FILE` placeholder in `src/interfaces/workers/message.worker.ts` so that the conversational FSM correctly routes messages in the file selection state.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [ ] `MessageWorkerDeps` interface updated to include `handleSpreadsheetFileSelection: HandleSpreadsheetFileSelection`.
  - [ ] `processMessageJob` switch case for `ONBOARDING_FILE` delegates to `HandleSpreadsheetFileSelection.execute()` instead of sending the placeholder.
  - [ ] The worker passes `rawMessage`, `userId`, `externalId`, `channel`, and `conversationState.statePayload` to the use case.
  - [ ] If `conversationState` is missing, the worker falls back to `RecoverCorruptedState` (existing pattern).
  - [ ] The `createMessageWorker` factory is updated to accept the new dependency.
  - [ ] `incomingMessage.worker.ts` and `telegram.webhook.ts` wiring is updated if they need to instantiate the new use case.
  - [ ] Unit tests for the message worker's `ONBOARDING_FILE` branch pass.

- **Technical dependencies (block the start):** T-4.02-03, T-4.02-05
- **Delivery dependencies (block the merge):** T-4.02-06

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the existing `ONBOARDING_START` wiring pattern in `message.worker.ts`. Ensure the dependency graph is updated in the DI composition root (usually `src/main.ts` or similar) so the use case is instantiated with all required ports and repositories.
