### Task T-4.02-06: Write tests and feature documentation

- **Description:** Write comprehensive unit and integration tests for all new components introduced by HU-4.02. Create or update the canonical feature documentation under `docs/features/`. Ensure `pnpm lint && pnpm typecheck && pnpm test` passes.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Unit tests for `HandleSpreadsheetFileSelection` with mocked `CloudStoragePort`, `IOAuthTokenRepository`, `IConversationStateRepository`, and `MessagingOutputPort`.
  - [x] Unit tests for `GoogleDriveFileDiscoveryAdapter` with mocked `fetch` (use `nock` or `msw` if already in the project; otherwise manual `jest.spyOn(global, 'fetch')`).
  - [x] Unit tests for the new `ONBOARDING_FILE` branch in `message.worker.ts`.
  - [x] Unit tests for all new onboarding copy functions.
  - [x] Feature document created at `docs/features/spreadsheet-file-selection.md` covering:
    - Overview and scope (in/out)
    - FSM state `ONBOARDING_FILE` description and transitions
    - Sequence diagram: user message → list/search/validate → state transition to `ONBOARDING_SHEET`
    - Adapter contract (`CloudStoragePort`)
    - Error handling matrix (no files, invalid selection, URL permission denied, API failure)
    - QA checklist for all five Gherkin scenarios
  - [x] `docs/features/README.md` index updated to include the new feature document.
  - [x] `pnpm lint`, `pnpm typecheck`, and `pnpm test` all pass green.

- **Technical dependencies (block the start):** T-4.02-02, T-4.02-04
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2 hours

- **Suggested role:** Backend / QA

- **Technical notes:**
  - Mock only at boundaries (network, DB, messaging). Never mock core business logic.
  - Follow the existing `docs/features/cloud-storage-connection.md` structure and tone.
  - Tests must have meaningful assertions (e.g., assert exact state transition, assert correct message text, assert payload contents).
  - The `docs/features/spreadsheet-file-selection.md` must explicitly document that the selected file is stored in `conversationStates.statePayload` until HU-4.04 creates the definitive `spreadsheet_configs` record.
