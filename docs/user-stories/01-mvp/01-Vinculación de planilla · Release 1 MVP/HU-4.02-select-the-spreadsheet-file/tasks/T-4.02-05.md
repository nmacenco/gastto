### Task T-4.02-05: Add onboarding copies for file selection flow

- **Description:** Extend `src/application/copies/onboarding.copies.ts` with all user-facing text strings required by the spreadsheet file selection flow. Keep the same functional style (arrow functions returning strings) used by the existing copies.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] `fileListPrompt(files: CloudFile[]): string` — returns a numbered list (1–5) plus a "None of these / search by name" option.
  - [ ] `noFilesFoundPrompt(): string` — returns a clear message explaining no compatible files were found and suggesting to verify the account or type the name manually.
  - [ ] `fileSelectedConfirmation(fileName: string): string` — returns a confirmation of the selected file name.
  - [ ] `invalidSelectionRePrompt(fileCount: number): string` — returns a re-prompt when the user sends an invalid number or unrecognized input.
  - [ ] `searchByNamePrompt(): string` — returns a prompt asking the user to type part of the file name.
  - [ ] `urlValidationFailed(): string` — returns a message when the pasted URL is invalid or the user lacks permissions.
  - [ ] `urlValidationSuccess(fileName: string): string` — returns a confirmation when a direct URL is validated successfully.
  - [ ] All copy functions are pure (no side effects) and accept only serializable arguments.
  - [ ] Unit tests verify the output strings contain expected placeholders and formatting.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.02-03, T-4.02-04

- **Estimation:** 1 hour

- **Suggested role:** Backend / QA

- **Technical notes:** Follow the existing `onboardingCopies` object style. Use Markdown bold (`*text*`) for Telegram emphasis. Do not hardcode provider names unless necessary; accept them as parameters.
