### Task T-4.05-06: Wire ONBOARDING_MAPPING in message worker

- **Description:** Add `InferColumnMapping` to `MessageWorkerDeps` and replace the placeholder in `message.worker.ts` (lines 165–168) for the `ONBOARDING_MAPPING` case. The worker must call `InferColumnMapping.execute()` with the job data and conversation state payload. The worker ONLY deserializes the state and delegates — all business logic stays in the use case. Register the use case in the worker composition (wherever `createMessageWorker` is called, likely `src/main.ts` or a DI container).

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [ ] `MessageWorkerDeps` includes `inferColumnMapping?: InferColumnMapping | null`.
  - [ ] `ONBOARDING_MAPPING` case calls `opts.inferColumnMapping.execute(...)` with correct arguments.
  - [ ] If `inferColumnMapping` is not provided, falls back to `onboardingCopies.onboardingPlaceholder()`.
  - [ ] The worker passes `userId`, `externalId`, `channel`, and `statePayload` from the job data and conversation state.
  - [ ] The use case is instantiated and injected in the worker composition root.
  - [ ] No business logic is added to the worker — it only delegates.

- **Technical dependencies (block the start):** T-4.05-05
- **Delivery dependencies (block the merge):** T-4.05-07

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Follow the pattern used for `ONBOARDING_VALIDATING_ACCESS` (lines 151–163 of `message.worker.ts`).
  - The `InferColumnMapping` use case must be instantiated with all its dependencies (token repo, spreadsheet config repo, column mapping repo, inference port, messaging port, token encryption, transition state).
  - Check where `createMessageWorker` is called to add the new dependency.
  - The `ONBOARDING_MAPPING` case currently groups with `ONBOARDING_CATEGORIES` (line 165) — split them so `ONBOARDING_CATEGORIES` keeps its placeholder.
  - Run `pnpm lint`, `pnpm typecheck`, and `pnpm test` after changes.
