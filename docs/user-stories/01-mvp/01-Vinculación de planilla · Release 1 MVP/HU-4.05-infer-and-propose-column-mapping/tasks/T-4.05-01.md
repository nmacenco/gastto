### Task T-4.05-01: Define column inference domain types and port

- **Description:** Create the `ConfidenceLevel` union type (`'alta' | 'baja'`), the `ColumnInferenceResult` interface (shape: `{ mappings: Array<{gasttoField, columnIndex, columnHeader, confidence}>, noHeaderFound: boolean, unmappedFields: GasttoField[] }`), and the `ColumnInferencePort` interface with `infer(headers: string[], sampleRows: string[][]) → Promise<ColumnInferenceResult>`. Additionally, update `FSM_TRANSITIONS` in `ConversationState.ts` to allow `ONBOARDING_MAPPING` self-transition (needed for the no-header sub-step where the system asks the user which row data starts at).

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [ ] `ConfidenceLevel` type is defined as `'alta' | 'baja'`.
  - [ ] `ColumnInferenceResult` interface has `mappings`, `noHeaderFound`, and `unmappedFields` fields with correct types.
  - [ ] `ColumnInferencePort` interface is defined with `infer` method signature.
  - [ ] `FSM_TRANSITIONS['ONBOARDING_MAPPING']` includes `'ONBOARDING_MAPPING'` (self-transition).
  - [ ] `canTransition('ONBOARDING_MAPPING', 'ONBOARDING_MAPPING')` returns `true`.
  - [ ] All existing FSM transition tests still pass.
  - [ ] No imports from Infrastructure or Application layers.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.05-02, T-4.05-05

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Follow existing port patterns in `src/domain/ports/` (e.g., `spreadsheetAccess.ts`).
  - `GasttoField` type already exists in `src/domain/entities/SpreadsheetConfig.ts`.
  - FSM transition change is backward-compatible (adds a new valid transition, does not remove any).
  - Run `pnpm typecheck` and `pnpm test` after changes.
