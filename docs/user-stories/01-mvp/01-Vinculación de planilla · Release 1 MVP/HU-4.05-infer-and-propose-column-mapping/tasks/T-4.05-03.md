### Task T-4.05-03: Implement Drizzle column mapping repository

- **Description:** Implement `DrizzleColumnMappingRepository` conforming to `IColumnMappingRepository` defined in `src/domain/ports/repositories.ts`. Implement three methods: `findBySpreadsheetId(spreadsheetId)` which queries `column_mappings` by FK and returns `ColumnMapping[]`; `upsertMany(mappings)` which performs `INSERT ... ON CONFLICT (spreadsheet_id, gastto_field) DO UPDATE` for batch persistence of inferred mappings; and `confirm(id)` which sets `confirmed_at = NOW()` for a given mapping. The DB column `gastto_field` must be mapped to the domain entity's `GasttoField` (capital G) to maintain compatibility with `RegisterExpense.ts` which already uses this naming.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] `findBySpreadsheetId` returns mapped `ColumnMapping[]` with correct field types.
  - [x] `findBySpreadsheetId` returns empty array when no mappings exist.
  - [x] `upsertMany` inserts new mappings in a single query.
  - [x] `upsertMany` updates existing mappings on conflict (same `spreadsheet_id` + `gastto_field`).
  - [x] `confirm(id)` sets `confirmedAt` to current timestamp.
  - [x] `confirm(id)` throws or returns error when mapping does not exist.
  - [x] Domain entity `GasttoField` (capital G) is correctly mapped from DB `gastto_field` (lowercase).
  - [x] Unit tests mock Drizzle ORM (following `DrizzleSpreadsheetConfigRepository.spec.ts` pattern).

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.05-05

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Follow the pattern in `src/infrastructure/db/repositories/DrizzleSpreadsheetConfigRepository.ts`.
  - DB schema already exists in `src/infrastructure/db/schema/index.ts` (`columnMappings` table).
  - `IColumnMappingRepository` is already defined in `src/domain/ports/repositories.ts`.
  - `ColumnMapping` interface in `src/domain/entities/SpreadsheetConfig.ts` uses `GasttoField` (capital G) — preserve this for compatibility with `RegisterExpense.ts:188`.
  - Use `onConflictDoUpdate` from Drizzle for `upsertMany`.
  - No migration needed — table already exists.
  - Run `pnpm typecheck` and `pnpm test` after implementation.
