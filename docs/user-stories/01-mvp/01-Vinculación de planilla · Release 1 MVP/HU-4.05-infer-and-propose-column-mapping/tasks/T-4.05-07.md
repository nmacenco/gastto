### Task T-4.05-07: Feature doc and remaining tests

- **Description:** Create the canonical feature documentation `docs/features/column-mapping-inference.md` following the project's feature doc conventions (see `docs/features/select-sheet.md` and `docs/features/validate-spreadsheet-access.md` as reference). The doc must cover: overview, scope (in/out), FSM states, flow sequence (all 5 Gherkin scenarios), adapters, API contracts (DTOs, ports), data model (column_mappings persistence), error handling table, and QA checklist. Update `docs/features/README.md` to add the new entry. Ensure all FSM transitions for the `ONBOARDING_MAPPING` flow have test coverage per testing guidelines rule #4.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] `docs/features/column-mapping-inference.md` exists and follows the project feature doc format.
  - [ ] Overview section explains the feature purpose and relationship to HU-4.05.
  - [ ] Scope section lists in-scope (inference, proposal, no-header detection, unmapped fields, multi-language) and out-of-scope (confirmation/correction — HU-4.06, categories — HU-4.07).
  - [ ] FSM states table documents `ONBOARDING_MAPPING` transitions.
  - [ ] Flow sequence covers all 5 Gherkin scenarios.
  - [ ] API contracts section documents `InferColumnMappingInput/Output/Deps` and `ColumnInferencePort`.
  - [ ] Error handling table covers token errors, missing config, missing preview, inference failures.
  - [ ] QA checklist covers happy paths and error paths.
  - [ ] `docs/features/README.md` includes the new entry with correct path and description.
  - [ ] All FSM transition tests for `ONBOARDING_MAPPING` exist (including self-transition).

- **Technical dependencies (block the start):** T-4.05-05, T-4.05-06
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** Backend / QA

- **Technical notes:**
  - Use `docs/features/TEMPLATE.md` if it exists, otherwise follow `select-sheet.md` format.
  - The feature doc is canonical — "No `docs/features/<feature>.md` = the feature does not exist" (per `docs/features/README.md`).
  - Update the index in `docs/features/README.md` to include the new entry.
  - Testing guidelines require FSM transition coverage (rule #4) — verify all `ONBOARDING_MAPPING` transitions are tested.
  - Run `pnpm lint`, `pnpm typecheck`, and `pnpm test` to ensure nothing is broken.
