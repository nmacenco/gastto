### Task T-4.03-01: Extend SpreadsheetPort with listSheets and define SheetInfo value object

- **Description:** Extend the existing `SpreadsheetPort` interface in the Domain layer to support sheet discovery. Add a `listSheets(fileId: string): Promise<SheetInfo[]>` method and define a `SheetInfo` value object with fields: `name: string`, `index: number` (zero-based or one-based per implementation convention). Also add a `getHeaders(fileId: string, sheetName: string): Promise<string[]>` method if not already present in the port. The use case needs these operations to implement the sheet selection flow.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [x] `SheetInfo` value object exists in `src/domain/entities/SheetInfo.ts` with fields: `name: string`, `index: number`.
  - [x] `SpreadsheetPort` in `src/domain/ports/services.ts` is extended with `listSheets(fileId: string): Promise<SheetInfo[]>`.
  - [x] `getHeaders(fileId: string, sheetName: string): Promise<string[]>` is present in `SpreadsheetPort` (already documented in ADR-004; verify existence or add it).
  - [x] No external library imports in the Domain layer.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.03-02, T-4.03-04

- **Estimation:** 0.5 hours

- **Suggested role:** Backend

- **Technical notes:** `SpreadsheetPort` already exists in `src/domain/ports/services.ts`. `getHeaders` is already declared in the current port; `listSheets` is the only addition needed. Follow the existing `Row`, `AppendResult`, and `CellValue` patterns in the same file.
