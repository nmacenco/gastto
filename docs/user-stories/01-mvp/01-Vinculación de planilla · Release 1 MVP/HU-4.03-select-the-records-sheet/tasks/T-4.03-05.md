### Task T-4.03-05: Add onboarding copies for sheet selection flow

- **Description:** Add the conversational copy strings needed by the `HandleSheetSelection` use case to `src/application/copies/onboarding.copies.ts`. The copies must cover: single-sheet auto-confirmation message, numbered sheet list with a "none of these / I don't know" option, header-based description for each sheet, fuzzy-match confirmation, sheet-not-found re-prompt, and transition message to mapping. Keep the same tone and formatting (Markdown for bold/italic) as existing copies.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] `onboardingCopies.sheetListPrompt(sheets)` returns a numbered list of sheet names with the last option for "I don't know".
  - [x] `onboardingCopies.singleSheetConfirmation(sheetName)` returns the auto-confirmation message for a single-sheet file.
  - [x] `onboardingCopies.sheetHeaderDescription(descriptions)` returns a brief description of each sheet's first row.
  - [x] `onboardingCopies.sheetSelectedConfirmation(sheetName)` returns the confirmation after a valid selection.
  - [x] `onboardingCopies.sheetNotFoundRePrompt(sheets)` returns the "not found" message plus the list again.
  - [x] `onboardingCopies.sheetMappingTransition()` returns the message indicating advancement to structure analysis.
  - [x] All copy functions are pure (no side effects) and typed.
  - [x] Unit tests verify exact output strings for given inputs.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-4.03-04, T-4.03-06

- **Estimation:** 0.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Follow the existing `onboardingCopies` pattern in `src/application/copies/onboarding.copies.ts`. The file already has functions for file selection; add sheet selection functions below them. Use the same language (Spanish) as the rest of the copies.
