### Task T-4.03-07: Write tests and feature documentation

- **Description:** Write comprehensive unit and integration tests for all components introduced in this User Story, and create/update the canonical feature documentation. Tests must cover the `GoogleSheetsAdapter` (mocked API calls), `HandleSheetSelection` use case (all 5 Gherkin scenarios plus error paths), `DrizzleSpreadsheetConfigRepository` (in-memory DB), and the message worker integration. The feature documentation must be created under `docs/features/select-sheet.md` following the project's feature documentation template.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Unit tests for `GoogleSheetsAdapter` verify `listSheets` and `getHeaders` with mocked `fetch` responses.
  - [ ] Unit tests for `HandleSheetSelection` cover:
    - single-sheet auto-confirmation,
    - multi-sheet selection by number,
    - selection by fuzzy name match,
    - "I don't know" header description flow,
    - invalid name re-prompt,
    - token expired / API failure paths.
  - [ ] Unit tests for `DrizzleSpreadsheetConfigRepository` verify `create`, `findByUserId`, and `updateAccessVerified`.
  - [ ] Integration tests for `message.worker.ts` verify the `ONBOARDING_SHEET` routing delegates correctly.
  - [ ] All tests pass with `pnpm test`.
  - [ ] Feature documentation `docs/features/select-sheet.md` is created and includes: scope, FSM states, flow sequence, adapter contracts, error handling table, and QA checklist.
  - [ ] `docs/features/README.md` is updated to include the new `select-sheet.md` entry.
  - [ ] `docs/architecture/data-model.md` is updated to document the Option A convention (placeholder `accessVerifiedAt` on creation, real validation in HU-4.04).

- **Technical dependencies (block the start):** T-4.03-02, T-4.03-06
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:** The project uses Vitest. Follow existing test patterns (`*.spec.ts` alongside the source file). Mock boundaries (network, filesystem, DB) only; never mock core business logic. The feature doc template is in `docs/features/TEMPLATE.md`. The `docs/features/README.md` index must stay in sync.
