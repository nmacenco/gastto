### Task T-0.04-07: Update data model and feature documentation

- **Description:** Update `docs/architecture/data-model.md` to document the `conversation_states` table, its columns, indexes, and relationship to `users`. Create `docs/features/conversation-state-management.md` describing the FSM, valid states, transitions, timeout behavior, and corrupted-state recovery. Ensure docs reflect the implemented behavior, not future plans. Add a TODO reference if any behavior is pending a later HU.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] `docs/architecture/data-model.md` documents `conversation_states` schema, indexes (`idx_conversation_states_expires`, `idx_conversation_states_current`), and FK to `users`.
  - [x] `docs/features/conversation-state-management.md` exists and covers:
    - FSM state list and transition map.
    - Persistence mechanism (Drizzle + Postgres).
    - Timeout and recovery behavior.
    - Clean Architecture boundary (route delegates, use case owns logic).
  - [x] No doc = blocked per `AGENTS.md` Ship Check.

- **Technical dependencies (block the start):** T-0.04-01, T-0.04-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:**
  - Follow `docs/documentation-guidelines.md` and `AGENTS.md` Documentation Sync rules.
  - Use the existing `TEMPLATE.md` in `docs/features/` if a format is required.
  - Reference ADR-003 (mentioned in `ConversationState.ts` comments) for FSM design context.
