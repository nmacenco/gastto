### Task T-0.04-05: Wire state management into message pipeline

- **Description:** Refactor the message processing pipeline to use the new state management use cases instead of direct repository access. Update `HandleStartCommand` to explicitly initialize or verify the user's conversation state. Update `RouteIncomingMessage` (or `message.worker.ts`) to delegate state transitions to `TransitionConversationState` and corrupted-state recovery to `RecoverCorruptedState`. Ensure the Fastify route handler (`telegram.webhook.ts`) and the BullMQ workers remain thin: they only deserialize, validate, and delegate. All business logic must live in the Application layer use cases.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] `HandleStartCommand` ensures a valid conversation state exists after `/start`.
  - [x] `message.worker.ts` delegates state transitions to `TransitionConversationState`.
  - [x] `message.worker.ts` delegates corrupted-state handling to `RecoverCorruptedState`.
  - [x] No repository calls remain in `message.worker.ts` or route handlers; only use-case calls.
  - [x] Clean Architecture boundary is respected: Interfaces layer = deserialization + validation + delegation.

- **Technical dependencies (block the start):** T-0.04-01, T-0.04-02, T-0.04-03
- **Delivery dependencies (block the merge):** T-0.04-06

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:**
  - `message.worker.ts` currently calls `opts.conversationRepo.transition` directly on lines 99 and 175; replace these with use-case calls.
  - `RouteIncomingMessage.spec.ts` and `telegram.webhook.spec.ts` may need updates to inject new use-case mocks.
  - Follow the existing dependency injection pattern (constructor-injected deps objects).
