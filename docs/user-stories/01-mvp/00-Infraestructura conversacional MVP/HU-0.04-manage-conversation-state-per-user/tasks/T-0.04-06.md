### Task T-0.04-06: Write integration tests for all 5 Gherkin scenarios

- **Description:** Create integration tests in `src/application/use-cases/conversation/` (or a dedicated `tests/integration/` folder) that exercise the full state management flow against a test database. Cover all 5 Gherkin scenarios: new user initialization, valid state transition, session persistence across simulated restarts, corrupted state recovery, and session timeout. Use the existing Vitest setup. Mock external services (LLM, Telegram API) at boundaries; test business logic and persistence with real Drizzle + test Postgres.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Scenario 1: New user message creates `ONBOARDING_START` state in DB.
  - [ ] Scenario 2: Valid action triggers correct state transition and subsequent messages use new context.
  - [ ] Scenario 3: State written yesterday is read back today and routing resumes mid-flow.
  - [ ] Scenario 4: Invalid state in DB triggers anomaly log, reset to `IDLE`, and recovery message.
  - [ ] Scenario 5: State with `expiresAt` in the past is picked up by timeout logic and user receives continue/restart prompt.
  - [ ] All tests pass with `pnpm test`.

- **Technical dependencies (block the start):** T-0.04-01, T-0.04-02, T-0.04-03, T-0.04-04, T-0.04-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** Backend / QA

- **Technical notes:**
  - Use the project's Vitest configuration (`pnpm test`).
  - Spin up a test DB or use an in-memory Postgres wrapper (e.g., `pg-mem` or Docker test DB).
  - Mock `MessagingOutputPort` to assert on sent messages without hitting Telegram.
  - Mock time for Scenario 5 (Vitest `vi.useFakeTimers()` or manipulate `expiresAt` directly).
  - Reference existing `.spec.ts` files for test style (e.g., `RouteIncomingMessage.spec.ts`).
