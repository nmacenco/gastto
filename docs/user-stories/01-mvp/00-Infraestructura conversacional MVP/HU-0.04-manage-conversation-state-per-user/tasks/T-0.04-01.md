### Task T-0.04-01: Implement conversation state Drizzle repository

- **Description:** Create `DrizzleConversationStateRepository` in `src/infrastructure/db/repositories/` that implements `IConversationStateRepository`. Implement `findByUserId`, `create`, `transition`, and `findExpired` using Drizzle ORM. The `transition` method must run as an atomic operation (use `db.transaction`). The repository must map between the Drizzle schema row shape and the domain `ConversationState` entity. Register the repository in the composition root.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] `DrizzleConversationStateRepository` implements `IConversationStateRepository`.
  - [x] `findByUserId` returns the mapped `ConversationState` or `null`.
  - [x] `create` inserts an initial `IDLE` row for a new user.
  - [x] `transition` updates `currentState`, `statePayload`, `expiresAt`, and `updatedAt` atomically.
  - [x] `findExpired` returns all rows where `expiresAt <= now()`.
  - [x] All methods use Drizzle queries; no raw SQL.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-0.04-02, T-0.04-03, T-0.04-04

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Schema is already defined in `src/infrastructure/db/schema/index.ts` (`conversationStates` table).
  - Port is already defined in `src/domain/ports/repositories.ts` (`IConversationStateRepository`).
  - Use `db.transaction(...)` for atomic updates per DB Conventions in `AGENTS.md`.
  - Reference existing `DrizzleUserRepository.ts` for style and patterns.
