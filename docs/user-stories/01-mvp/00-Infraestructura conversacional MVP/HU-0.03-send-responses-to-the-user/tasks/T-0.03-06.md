### Task T-0.03-06: Write unit tests for sender, retry, chunking, and error handling

- **Description:** Create comprehensive unit tests for the messaging adapter using Vitest. Mock the Telegram Bot API HTTP responses to cover: successful send, long message splitting, exponential backoff retry on 5xx, no-retry on 4xx, and logging assertions. Ensure 100% branch coverage for the adapter's decision paths.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Test: successful single message send returns success and logs correctly.
  - [ ] Test: message longer than 4096 chars is split and each fragment is sent.
  - [ ] Test: 5xx response triggers 3 retries with correct delays, then returns failure.
  - [ ] Test: 400/403 response returns failure immediately with no retries.
  - [ ] Test: logging spy confirms structured logs for all scenarios.
  - [ ] All tests pass with `pnpm test`.

- **Technical dependencies (block the start):** T-0.03-03, T-0.03-04, T-0.03-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Use Vitest as specified in `AGENTS.md`. Mock `fetch` or the HTTP client at the boundary. Use `vi.useFakeTimers()` to test backoff delays without waiting. Place tests alongside the adapter or in `tests/unit/infrastructure/adapters/telegram/` depending on project convention.
