### Task T-0.02-07: Write unit tests covering all 4 Gherkin scenarios

- **Description:** Write comprehensive Vitest tests that verify the complete message processing pipeline for all four acceptance criteria scenarios: incoming text message, unsupported type, malformed payload, and rapid succession ordering. Tests should mock external dependencies (Telegram API, queue) and focus on behavior at the Application and Infrastructure boundaries.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Test for Scenario 1: assert parser extracts correct fields and router dispatches to text handler.
  - [ ] Test for Scenario 2: assert unsupported payload returns the friendly message copy.
  - [ ] Test for Scenario 3: assert malformed payload logs error, returns 200, and does not throw.
  - [ ] Test for Scenario 4: assert 3 rapid messages are processed in order with correct individual responses.
  - [ ] All tests pass with `pnpm test`.
  - [ ] Coverage report shows the message routing module is covered.

- **Technical dependencies (block the start):** T-0.02-04, T-0.02-05, T-0.02-06
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2 hours

- **Suggested role:** QA / Backend

- **Technical notes:** Use `vitest` with inline mocks. Prefer testing through the public use-case API rather than internal parser internals. Run `pnpm test:coverage` to validate coverage.
