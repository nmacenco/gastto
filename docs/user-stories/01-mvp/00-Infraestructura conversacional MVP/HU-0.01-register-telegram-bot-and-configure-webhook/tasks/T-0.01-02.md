### Task T-0.01-02: Create Fastify webhook endpoint route and controller

- **Description:** Create a POST route in the Fastify application to receive Telegram webhook updates. Implement a controller that parses the incoming JSON payload and dispatches it to the appropriate application use case. Ensure the endpoint is publicly accessible via HTTPS and returns HTTP 200 OK to acknowledge receipt.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] POST endpoint exists at a defined public URL (e.g., `/webhook/telegram`) and is reachable via HTTPS.
  - [x] Incoming JSON payload is parsed and validated against the expected Telegram update schema.
  - [x] Endpoint responds with HTTP 200 OK for valid requests.

- **Technical dependencies (block the start):** T-0.01-00
- **Delivery dependencies (block the merge):** T-0.01-03, T-0.01-04, T-0.01-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Use Fastify route definitions. Consider using Zod for payload schema validation. Ensure the route is registered before application startup.
