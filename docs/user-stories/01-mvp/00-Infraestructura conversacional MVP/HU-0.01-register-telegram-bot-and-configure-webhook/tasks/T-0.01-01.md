### Task T-0.01-01: Register Telegram bot with BotFather and secure API token

- **Description:** Register the bot in Telegram via BotFather, obtaining the definitive public name, username, and API token. Store the token in the project's secret manager (e.g., Fly.io secrets or Vault). Ensure the token is never committed to source code or version control.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [ ] Bot is registered in Telegram with a definitive name and username aligned with the product branding.
  - [ ] API token is obtained and stored in the project's secret manager.
  - [x] Token is not present in source code, `.env` files checked into version control, or any other committed file.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-0.01-04

- **Estimation:** 1 hour

- **Suggested role:** DevOps / Backend

- **Technical notes:** Refer to `AGENTS.md` for the project's secret management conventions. Use Fly.io secrets (`flyctl secrets set`) or the configured vault provider.
