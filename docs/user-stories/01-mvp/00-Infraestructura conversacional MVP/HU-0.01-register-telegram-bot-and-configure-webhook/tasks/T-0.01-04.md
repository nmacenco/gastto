### Task T-0.01-04: Configure production webhook via Telegram API

- **Description:** Using the stored API token, call the Telegram Bot API `setWebhook` method to point to the production HTTPS endpoint. Verify that Telegram responds with status `ok` and that the webhook is active. Ensure the endpoint URL uses HTTPS.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] Telegram Bot API call to `setWebhook` returns `ok`.
  - [x] Webhook URL points to the production HTTPS endpoint.
  - [x] Webhook status is verified as active via `getWebhookInfo`.

- **Technical dependencies (block the start):** T-0.01-01, T-0.01-02
- **Delivery dependencies (block the merge):** T-0.01-06

- **Estimation:** 1 hour

- **Suggested role:** DevOps / Backend

- **Technical notes:** Use `curl` or an HTTP client to invoke `https://api.telegram.org/bot<token>/setWebhook`. Include the secret token in the `secret_token` parameter if supported by the adapter.
