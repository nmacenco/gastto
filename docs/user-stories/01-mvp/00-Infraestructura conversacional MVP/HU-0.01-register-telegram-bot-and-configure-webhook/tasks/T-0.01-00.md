### Task T-0.01-00: Bootstrap Fastify project skeleton and core infrastructure

- **Description:** Scaffold the base Fastify application with TypeScript, including the plugin registration system, environment configuration validation (Zod), structured logging (Pino), and error tracking (Sentry). Create the foundational folder structure under `src/` aligned with Clean Architecture layers (domain, application, infrastructure, interfaces). Add a basic health-check route to verify the server starts and binds to the correct port. This task establishes the runtime contract that all subsequent HUs will build upon.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Fastify server starts successfully with TypeScript compilation.
  - [ ] Environment variables are loaded and validated with Zod at startup; the app fails fast on invalid config.
  - [ ] Pino logger is configured and injected into the Fastify instance.
  - [ ] Sentry initialization is wired into error handling.
  - [ ] Basic health-check route (`GET /health`) responds with 200.
  - [ ] Clean Architecture folder structure exists under `src/` (domain, application, infrastructure, interfaces).

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-0.01-02

- **Estimation:** 2 hours

- **Suggested role:** Backend / DevOps

- **Technical notes:** Refer to `AGENTS.md` for the exact stack versions and conventions. Do not implement business logic here—only the runtime scaffold, config, and wiring that later tasks will consume.
