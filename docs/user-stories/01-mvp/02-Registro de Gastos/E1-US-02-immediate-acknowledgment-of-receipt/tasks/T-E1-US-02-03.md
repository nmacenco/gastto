### Task T-E1-US-02-03: Implement immediate acknowledgment use case

- **Description:** Create the `SendImmediateAcknowledgement` use case in the Application layer. The use case decides when to send an acknowledgment, builds a brief acknowledgment copy that is visually/textually differentiated from the final expense summary, and delegates delivery to the `MessagingOutputPort`. This keeps all business logic in the Application layer; the route handler (Interfaces layer) only deserializes, validates, and delegates.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] `SendImmediateAcknowledgement` use case exists in `src/application/use-cases/conversation/`.
  - [x] It accepts the `chatId`, `channel`, and optional user identity.
  - [x] It returns the result of the send operation (success or failure).
  - [x] The acknowledgment copy is clearly different from the final interpreted summary.
  - [x] Unit tests cover success and failure paths.
  - [x] The use case has no direct dependency on Telegram or WhatsApp APIs; it only uses `MessagingOutputPort`.

- **Technical dependencies (block the start):** T-E1-US-02-02
- **Delivery dependencies (block the merge):** T-E1-US-02-04

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Reference: `MessagingOutputPort` in `src/application/ports/output/messaging.port.ts`.
  - Acknowledgment copy can live in `src/application/copies/shared.copies.ts` (e.g., `processingAcknowledgment()` already exists; refine it if needed).
  - Do not perform identity resolution, conversation state lookup, or expense interpretation here — that belongs to downstream workers.
