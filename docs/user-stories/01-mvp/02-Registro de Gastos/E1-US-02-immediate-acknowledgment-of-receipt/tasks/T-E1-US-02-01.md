### Task T-E1-US-02-01: Add external message ID to NormalizedPayload and job data

- **Description:** Add `externalMessageId` to the `NormalizedPayload` domain contract, the `IncomingMessageJobData` type, and the `ProcessMessageJobData` type. Update `TelegramPayloadParser` to extract `message.message_id` from the raw Telegram payload and surface it as `externalMessageId`. Update existing unit and type-level tests to assert the new field. This field is the foundation for the idempotency mechanism.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [x] `NormalizedPayload` includes a required or optional `externalMessageId` field.
  - [x] `IncomingMessageJobData` carries `externalMessageId` as JSON-serializable data.
  - [x] `ProcessMessageJobData` carries `externalMessageId`.
  - [x] `TelegramPayloadParser` extracts `message.message_id` and maps it to `externalMessageId`.
  - [x] Existing tests compile and pass after the contract change.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-E1-US-02-02, T-E1-US-02-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:**
  - Reference files: `src/domain/ports/messaging.ts`, `src/application/ports/IncomingMessageJob.ts`, `src/application/ports/ProcessMessageJob.ts`, `src/infrastructure/adapters/telegram/TelegramPayloadParser.ts`.
  - Keep `externalMessageId` as a string in job data to avoid precision loss of large numeric IDs.
  - Run tests with `pnpm test`.
