### Task T-E1-US-02-02: Define idempotency key value object and repository port

- **Description:** Create an immutable `ProcessedMessageKey` value object in the Domain layer that combines `channel` and `externalMessageId`. Define the `IProcessedMessageRepository` driven port with `exists(key): Promise<boolean>` and `markAsProcessed(key): Promise<void>` operations. Keep the port agnostic of storage technology so infrastructure adapters can provide Redis, in-memory, or database implementations.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [x] `ProcessedMessageKey` value object exists in `src/domain/value-objects/`.
  - [x] Factory method validates that both `channel` and `externalMessageId` are non-empty.
  - [x] `IProcessedMessageRepository` port exists in `src/domain/ports/` or `src/application/ports/`.
  - [x] Port interface has `exists` and `markAsProcessed` methods.
  - [x] Unit tests cover key creation and equality.

- **Technical dependencies (block the start):** T-E1-US-02-01
- **Delivery dependencies (block the merge):** T-E1-US-02-03, T-E1-US-02-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:**
  - Follow the union/value-object pattern used by `MessageType.ts`.
  - The key can be represented as `{ channel: 'telegram' | 'whatsapp'; externalMessageId: string }`.
  - Keep the repository port on the Domain/Application boundary so the use case depends on it.
