### Task T-E1-US-02-05: Implement Redis idempotency repository

- **Description:** Implement `RedisProcessedMessageRepository` in the Infrastructure layer. It must satisfy the `IProcessedMessageRepository` port using Redis with an explicit TTL (e.g., 24 hours). `exists()` returns true if the key is present; `markAsProcessed()` writes the key atomically. Write unit tests using a mocked Redis client so no real Redis connection is required in the test suite.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] `RedisProcessedMessageRepository` exists in `src/infrastructure/redis/` or `src/infrastructure/db/repositories/`.
  - [x] It implements `IProcessedMessageRepository`.
  - [x] Keys are namespaced (e.g., `processed_message:<channel>:<externalMessageId>`).
  - [x] `markAsProcessed` sets a TTL on the key.
  - [x] `exists` returns `true` for previously marked keys and `false` otherwise.
  - [x] Unit tests pass with a mocked Redis client.

- **Technical dependencies (block the start):** T-E1-US-02-02
- **Delivery dependencies (block the merge):** T-E1-US-02-06

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Reference existing Redis helpers in `src/infrastructure/redis/`.
  - TTL choice: 24 hours is usually enough to cover Telegram retry windows while avoiding unbounded growth.
  - Use Redis `SET ... EX` or `setEx` for atomic write+TTL.
