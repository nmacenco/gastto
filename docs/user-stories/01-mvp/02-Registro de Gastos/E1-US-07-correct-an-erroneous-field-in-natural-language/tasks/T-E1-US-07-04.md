### Task T-E1-US-07-04: Add correction copies

- **Description:** Extend `src/application/copies/expense.copies.ts` with the user-facing messages the correction flow needs, following the existing copy style (Spanish, markdown, inline action hints). Add: `correctionApplied(field, value)` confirming a changed field, `correctionCycleLimitReached()` offering confirm or cancel once the 5-cycle limit is hit, and `correctionHighAmountConfirmation()` requesting explicit confirmation for an unusually high corrected amount. Reword `expenseCorrectionPrompt()` so it invites natural-language corrections ("no, fueron 15", "ponlo en transporte", "fue ayer") and states that several fields can be corrected in a single message. Reuse the existing `ambiguousResponse()` ("¿Querías confirmar, corregir o cancelar el registro?") for the uninterpretable case and `updatedSummary()` for the re-presented summary.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] `correctionApplied(field, value)` produces a clear confirmation message per field
  - [x] `correctionCycleLimitReached()` offers confirm or cancel with the current state
  - [x] `correctionHighAmountConfirmation()` requests explicit confirmation
  - [x] `expenseCorrectionPrompt()` includes natural-language examples and mentions multi-field corrections
  - [x] `ambiguousResponse()` and `updatedSummary()` are reused without duplication
  - [x] Copy unit tests cover each new message (`expense.copies.spec.ts`)

- **Technical dependencies (block the start):** T-E1-US-07-03
- **Delivery dependencies (block the merge):** T-E1-US-07-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Keep the existing `expenseSummary`/`updatedSummary` helpers untouched where possible. Follow the tone and formatting of the current file (bold labels, bullet lists, "¿Confirmamos?" action line). Copies are plain functions in `src/application/copies/expense.copies.ts`; unit tests live in `expense.copies.spec.ts`.
