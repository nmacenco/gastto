### Task T-E1-US-07-06: Document the expense-correction feature

- **Description:** Create the canonical feature document `docs/features/expense-correction.md` following `docs/features/TEMPLATE.md`. It must document the implemented behavior only: the correction entry points (from `EXPENSE_REVIEW` non-confirm/cancel text and from the `EXPENSE_CORRECTING` state via the "corregir" action), the fields that can be corrected (amount, currency, category, date), the multi-field single-message rule, the unusually-high-value explicit confirmation, the 5-cycle limit, and the "no save until explicit confirmation" rule (E1-US-08). Include the architectural boundary contract: the worker/route only deserializes and delegates, `CorrectExpenseUseCase` owns the business logic, and the LLM is reached only through the `LLMPort`. Reference the tests that cover each acceptance criterion. Update the `docs/features/README.md` index.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] `docs/features/expense-correction.md` exists and follows `docs/features/TEMPLATE.md`
  - [ ] It documents entry points, corrected fields, multi-field rule, high-amount confirmation, cycle limit, and the no-save-until-confirm rule
  - [ ] It documents the Clean Architecture boundary (worker delegates, use case owns logic, LLM behind port)
  - [ ] It maps every Gherkin scenario to its covering test
  - [ ] `docs/features/README.md` index is updated with the new entry

- **Technical dependencies (block the start):** T-E1-US-07-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** Backend / Technical Writer

- **Technical notes:** Reference `docs/features/expense-summary-review.md` and `docs/features/clarification-request.md` for style and structure. Per `AGENTS.md`: no feature without canonical `docs/features/*.md`; update the directory `README.md` whenever adding a feature doc.
