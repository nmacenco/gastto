### Task T-E1-US-07-07: Write unit and integration tests for all correction scenarios

- **Description:** Write tests covering every Gherkin scenario from the user story plus the Definition of Done requirements. Unit tests in `src/application/use-cases/expense/CorrectExpenseUseCase.spec.ts`: amount correction, category correction (via mocked `ICategoryClassifier`), date correction ("fue ayer"), multi-field single-message correction, unusually-high-value explicit confirmation, uninterpretable message, and the 5-cycle limit. Integration tests in `src/interfaces/workers/message.worker.spec.ts`: end-to-end through the worker for the `EXPENSE_CORRECTING` state and for direct correction text in `EXPENSE_REVIEW`, asserting exactly one updated-summary message for multi-field corrections, the `not_interpretable` orientation message, and the `cycle_limit` path. Mock only at infrastructure boundaries (LLM adapter, Telegram API, database) — never the use case business logic.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Unit test for amount correction updates the amount to 15 EUR
  - [ ] Unit test for category correction resolves through the classifier to the user's normalized category
  - [ ] Unit test for date correction ("fue ayer") resolves to the previous day
  - [ ] Unit test for multi-field correction applies both fields in one execution
  - [ ] Unit test for unusually-high value requests explicit confirmation before applying
  - [ ] Unit test for uninterpretable message returns `'not_interpretable'` and modifies nothing
  - [ ] Unit test for exceeding 5 correction cycles returns `'cycle_limit'`
  - [ ] Integration tests cover both entry points (`EXPENSE_CORRECTING` state and direct text in `EXPENSE_REVIEW`)
  - [ ] Integration test asserts a single updated summary message for multi-field corrections
  - [ ] All tests pass with `pnpm test`; no business logic is mocked

- **Technical dependencies (block the start):** T-E1-US-07-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** QA / Backend

- **Technical notes:** Follow existing patterns in `message.worker.spec.ts` (clarification tests around lines 400–496) and `RegisterExpense.spec.ts`. Mock the `LLMPort` adapter and repositories at the port boundary. Run with `pnpm test`. See `docs/testing/guidelines.md` for coverage and mock-boundary conventions.
