### Task T-E1-US-07-05: Wire the correction flow into the message worker

- **Description:** Wire the correction use case into the Telegram/WhatsApp pipeline in `src/interfaces/workers/message.worker.ts`. (1) Add a `case 'EXPENSE_CORRECTING'` branch to `routeByState()` (the state exists in the FSM but currently has no handler, so the default branch would reset it). Implement a `handleExpenseCorrection()` helper that deserializes and validates the `ExpenseCorrectionState` from the stored payload, delegates to `CorrectExpenseUseCase`, and sends the appropriate copy per outcome (`not_interpretable` → `ambiguousResponse()`, `cycle_limit` → `correctionCycleLimitReached()`, success → present the updated summary). (2) In `handleExpenseReview()`, the current non-confirm/non-cancel branch blindly sends `ambiguousResponse()`; change it to attempt a correction through `CorrectExpenseUseCase` first (so "no, fueron 15" typed directly on the summary works), falling back to `ambiguousResponse()` only on the `not_interpretable` outcome. (3) Re-present the updated summary exactly once per correction through the existing `presentExpenseSummary()` path (`GenerateExpenseSummaryUseCase` + presenter with inline actions), which also surfaces the high-amount warning and explicit-confirmation prompt. (4) Register `CorrectExpenseUseCase` in the DI container (`src/bootstrap/buildDependencies.ts`) and add it to `MessageWorkerDeps`.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] A message while in `EXPENSE_CORRECTING` is routed to `handleExpenseCorrection()` and not to the default recovery branch
  - [x] A correction typed directly in `EXPENSE_REVIEW` is interpreted as a correction, not blindly answered with `ambiguousResponse()`
  - [x] `not_interpretable` outcomes send `ambiguousResponse()` and change no data or state
  - [x] `cycle_limit` outcomes send `correctionCycleLimitReached()` and keep the current state
  - [x] Successful corrections present a single updated summary message (assert only one send)
  - [x] The worker contains no business logic — it only deserializes, validates, delegates, and presents
  - [x] `CorrectExpenseUseCase` is wired in `buildDependencies.ts` and `MessageWorkerDeps`

- **Technical dependencies (block the start):** T-E1-US-07-03, T-E1-US-07-04
- **Delivery dependencies (block the merge):** T-E1-US-07-06, T-E1-US-07-07

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:** Clean Architecture boundary guardrail — the route handler/worker only deserializes, validates, and delegates; all business logic lives in `CorrectExpenseUseCase`; the LLM is reached only through the port. Mirror the existing `handleExpenseReview`/`handleClarification` helpers (payload validation, `opts.logger.error` with structured objects, `fallbackError()` on corrupt payload). Single-summary assertion: capture `messaging.sendMessage` calls and assert exactly one summary presentation per correction. Worker registration follows `registerWorkers.ts` / `buildDependencies.ts` patterns.
