### Task T-E1-US-12-05: Route retry and reconfiguration commands

- **Description:** Extend the message worker and intent routing so `reintentar` is handled only while the user has an active `EXPENSE_SAVING_RETRY` payload, and `reconfigurar` enters the existing spreadsheet mapping reconfiguration flow. The worker must delegate to Application use cases and must handle expired or missing retry state with a clear recovery response.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] `reintentar` loads the preserved expense payload and delegates the retry without re-running unrelated NLP or creating a new expense.
  - [x] A successful retry sends the standard E1-US-10 confirmation and clears the retry state.
  - [x] A second failure follows the terminal manual-copy fallback and clears or expires the retry state safely.
  - [x] `reconfigurar` delegates to the existing mapping/onboarding flow without placing business rules in the worker.
  - [x] Retry after the 10-minute TTL receives a clear recovery path and cannot save stale or missing data.
  - [x] Worker tests cover text routing, missing dependencies, expired state, successful retry, and failed retry.

- **Technical dependencies (block the start):** T-E1-US-12-04
- **Delivery dependencies (block the merge):** T-E1-US-12-06

- **Estimation:** 2.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Update `src/interfaces/workers/message.worker.ts` and its specs. The worker is an Interfaces adapter: it only deserializes, validates, and delegates. Keep retry and reconfiguration business logic in Application use cases behind ports; do not call spreadsheet adapters directly from the worker.
