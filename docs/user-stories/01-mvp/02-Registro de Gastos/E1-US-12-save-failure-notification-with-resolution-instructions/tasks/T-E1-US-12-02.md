### Task T-E1-US-12-02: Map provider failures to typed categories

- **Description:** Update the spreadsheet adapters used by expense saving to translate Google Sheets and Excel Online failures into the typed categories from T-E1-US-12-01. Cover network timeouts, HTTP 401/403 or equivalent revoked-access responses, and missing sheets or columns caused by stale mappings. Preserve confirmed successful append results separately from failures.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] Network failures from each supported provider become `NETWORK_ERROR`.
  - [x] Expired-token and permission-denied responses become `AUTH_ERROR`.
  - [x] Missing sheet, column, or stale mapping responses become `STRUCTURE_ERROR`.
  - [x] A provider failure never returns append success metadata or triggers the E1-US-10 confirmation path.
  - [x] Adapter tests cover the mappings for both provider boundaries and preserve existing successful append behavior.

- **Technical dependencies (block the start):** T-E1-US-12-01, E4 spreadsheet access and write services
- **Delivery dependencies (block the merge):** T-E1-US-12-04, T-E1-US-12-06

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:** Inspect `GoogleSheetsAdapter`, `ExcelOnlineAdapter`, and their existing specs under `src/infrastructure/adapters/sheets/`. Mock only provider/network boundaries. Do not leak tokens, raw provider payloads, or stack traces into user-facing messages.
