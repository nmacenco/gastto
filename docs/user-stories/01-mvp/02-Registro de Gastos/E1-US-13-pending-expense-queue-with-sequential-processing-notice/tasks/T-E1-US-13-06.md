### Task T-E1-US-13-06: Coordinate queue behavior with reminders and undo

- **Description:** Extend Application-level handling of non-financial review replies, two-stage 10-minute expiry, and E1-US-11 undo so queued expenses remain intact and resume in the required order. Add queue-aware reminder and expiration copy, and suspend/recover queued review presentation around the undo workflow.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] A non-financial message during review with queued work repeats the specified reminder without changing the active draft or queue, including repeated attempts.
  - [ ] The first 10-minute expiry reminder includes the pending count; the second expires only the active draft and advances to the oldest queued expense.
  - [ ] Undo targets the most recently saved expense, preserves the queued review context, and resumes it only after the E1-US-11 outcome is completed.
  - [ ] Timeout and undo failures are logged with structured context and cannot drop or reorder queue entries.
  - [ ] Unit/integration tests cover the reminder, expiry-to-next-expense, and undo-with-queue paths.

- **Technical dependencies (block the start):** T-E1-US-13-02, T-E1-US-13-03
- **Delivery dependencies (block the merge):** T-E1-US-13-07

- **Estimation:** 5 hours

- **Suggested role:** Backend

- **Technical notes:** Follow `docs/features/conversation-state-management.md`, `docs/features/undo-last-expense.md`, and the existing timeout contract. Application use cases own the rules and call output ports; adapters only validate and delegate. Coordinate the E1-US-11 ownership boundary before merge.
