### Task T-E1-US-13-07: Verify and document sequential expense processing

- **Description:** Add cross-layer integration coverage for the three-expense FIFO journey through `IDLE`, capacity rejection and release, non-financial replies, timeout advancement, and undo resumption. Update the canonical feature and state/data documentation to describe only the implemented queue behavior and channel parity.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] An integration test proves three separate rapid messages are processed sequentially to `IDLE` with correct message ordering and no lost expense.
  - [ ] Tests prove the 2-pending limit, capacity release, cancellation, timeout, non-financial repeat, and undo integration behavior.
  - [ ] Boundary mocks are limited to messaging, database, queue, and external spreadsheet/LLM services; core queue orchestration uses real logic.
  - [ ] Relevant `docs/features/` documents, `docs/architecture/fsm-states.md`, and `docs/architecture/data-model.md` describe the delivered design; feature README indexes are updated if a feature document is added or renamed.
  - [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass.

- **Technical dependencies (block the start):** T-E1-US-13-03, T-E1-US-13-04, T-E1-US-13-05, T-E1-US-13-06
- **Delivery dependencies (block the merge):** None

- **Estimation:** 5 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Follow `docs/testing/guidelines.md` and `AGENTS.md` Ship Check. Preserve the documented Clean Architecture boundary in code review across all touched handlers and workers.
