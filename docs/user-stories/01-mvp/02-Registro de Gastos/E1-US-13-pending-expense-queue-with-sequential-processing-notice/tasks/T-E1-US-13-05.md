### Task T-E1-US-13-05: Continue the queue after confirmation or cancellation

- **Description:** Integrate the queue-advancement orchestration into the existing confirmation and cancellation application flows. Ensure save-location confirmation or cancellation copy is sent first, then the pending-count notice, then the next review summary; retain the active queue when correction or clarification is in progress.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] Confirming an expense saves it through the existing E1-US-10 flow and then starts the oldest queued expense when one exists.
  - [ ] Cancelling an active expense discards only that draft, then starts the oldest queued expense when one exists.
  - [ ] The notice is never sent before the save/cancellation outcome and the next review uses the existing extraction/review flow.
  - [ ] Correction and clarification preserve the active draft and queue unchanged.
  - [ ] Tests cover confirmation and cancellation with one and two queued expenses, including the final `IDLE` closing message.

- **Technical dependencies (block the start):** T-E1-US-13-02, T-E1-US-13-03
- **Delivery dependencies (block the merge):** T-E1-US-13-07

- **Estimation:** 4 hours

- **Suggested role:** Backend

- **Technical notes:** Integrate with `docs/features/expense-confirmation.md`, `docs/features/expense-cancellation.md`, and E1-US-10. Use the same Application output ports for all channels; keep `message.worker.ts` thin.
