### Task T-E1-US-05-02: Implement single-missing-field clarification trigger

- **Description:** Implement the business logic that detects when exactly one piece of data (amount or currency) is missing or ambiguous and triggers the clarification flow. This is already partially implemented in `RegisterExpense.interpret()` which returns `{ status: 'needs_clarification', missingField: 'monto' | 'moneda' }`. Ensure the priority order is enforced: amount > currency > category. For the category ambiguity case (low confidence), the story specifies no additional question is asked — the category is shown as editable in the review step (E1-US-06). Verify this path works correctly.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] When amount is missing, `interpret()` returns `needs_clarification` with `missingField: 'monto'`
  - [x] When amount is present but currency is missing/ambiguous, returns `needs_clarification` with `missingField: 'moneda'`
  - [x] When category confidence is low, the flow proceeds to `EXPENSE_REVIEW` without an additional clarification question
  - [x] The clarification state (`missingField`, partial extracted data, `rawMessage`) is persisted via `transitionToClarifying()` with a 30-minute TTL
  - [x] Unit tests cover the priority enforcement for all combinations of missing fields

- **Technical dependencies (block the start):** T-E1-US-05-01
- **Delivery dependencies (block the merge):** T-E1-US-05-03, T-E1-US-05-07

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:** The core logic is in `src/application/use-cases/expense/RegisterExpense.ts` (lines 89–124). The `transitionToClarifying()` private method at line 260 already persists the state. No bootstrap needed — the FSM, conversation repository, and `EXPENSE_CLARIFYING` state already exist. Only verify and harden the existing implementation.
