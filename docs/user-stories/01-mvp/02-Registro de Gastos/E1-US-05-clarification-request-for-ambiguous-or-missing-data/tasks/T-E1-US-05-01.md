### Task T-E1-US-05-01: Define clarification state payload domain types

- **Description:** Create a dedicated domain value object that formalizes the state payload shape for the `EXPENSE_CLARIFYING` FSM state. Currently the payload is stored as `Record<string, unknown>` with loosely typed fields. Introduce a typed `ExpenseClarificationState` value object that captures: `missingField` (`'monto' | 'moneda'`), `partialExtracted` (the partially parsed expense data from the LLM), and `rawMessage` (the original user message). This enables type-safe access across the Application and Interfaces layers.

- **Architectural layer:** `Domain`

- **Acceptance criteria:**
  - [x] `ExpenseClarificationState` value object exists in `src/domain/value-objects/`
  - [x] It exposes typed fields: `missingField`, `partialExtracted`, `rawMessage`
  - [x] It is serializable to `Record<string, unknown>` for storage in `conversation_states.statePayload`
  - [x] It can be reconstructed from the serialized payload
  - [x] It is immutable and validated on construction
  - [x] Unit tests verify construction, serialization, and deserialization

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-E1-US-05-02, T-E1-US-05-03

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Place the value object at `src/domain/value-objects/expense-clarification-state.ts`. Follow the existing pattern in `src/domain/value-objects/` for serialization. The shape is already implicitly defined by `RegisterExpense.ts:transitionToClarifying()` — this task formalizes it into a named type.
