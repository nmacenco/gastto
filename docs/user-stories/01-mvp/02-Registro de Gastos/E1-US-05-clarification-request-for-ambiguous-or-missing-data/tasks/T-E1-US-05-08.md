### Task T-E1-US-05-08: Write integration tests for clarification scenarios

- **Description:** Write integration and unit tests covering all Gherkin scenarios from the user story. Existing tests in `message.worker.spec.ts` already cover basic clarification scenarios (missing amount sends question, currency clarification, updated summary on resolution, "still needs more info"). The missing test scenarios are: (1) interruption — new expense while in `EXPENSE_CLARIFYING` state cancels previous flow (DoD requirement), (2) invalid response reformulation — user responds "no sé" and receives reformulated question with options, (3) sequential missing fields — user provides amount but currency is still missing, then system asks for currency. All tests must mock only at network boundaries (Telegram API, LLM, database) and use meaningful assertions.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Integration test for "new expense interrupts previous clarification" covers: user in `EXPENSE_CLARIFYING`, sends new expense → previous flow cancelled → notification sent → new expense processed
  - [ ] Test for "invalid response reformulation" covers: user responds "no sé" to currency clarification → reformulated question with options is sent
  - [ ] Test for "sequential missing fields" covers: user provides amount when both amount and currency are missing → system asks for currency next
  - [ ] All 6 Gherkin scenarios from the user story are covered by at least one test
  - [ ] Tests use Vitest and follow the project's testing conventions

- **Technical dependencies (block the start):** T-E1-US-05-07
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** QA / Backend

- **Technical notes:** Tests live in `src/interfaces/workers/message.worker.spec.ts` (existing clarification tests at lines 400–496). The interruption test must set up a `EXPENSE_CLARIFYING` state and send a message that looks like a new complete expense. For the reformulation test, mock the expense repository to return previously used currencies. Refer to `src/application/use-cases/expense/RegisterExpense.spec.ts` for existing patterns. Run tests with `pnpm test`.
