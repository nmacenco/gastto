### Task T-E1-US-05-03: Handle user response to clarification

- **Description:** Implement the handler that processes the user's answer to a clarification question. This is already partially implemented in `handleClarification()` in `message.worker.ts` (lines 701–743). The handler reads the state payload, enriches the original message with the user's response, and re-interprets via `RegisterExpense.interpret()`. Ensure the sequential flow works: when the user provides the missing amount, if currency is still missing, the next `interpret()` call returns `needs_clarification` with `missingField: 'moneda'` rather than proceeding to review. When all required data is present, transition to `EXPENSE_REVIEW`.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] When user provides the missing data and all fields are present, the enriched message is re-interpreted and transitions to `EXPENSE_REVIEW`
  - [x] When user provides one missing field but another is still missing (e.g., provides amount but currency is also needed), the system asks for the next missing field
  - [x] The original message context is preserved across clarification turns (the state payload keeps accumulating responses)
  - [x] Tests cover: (a) clarification resolves to review, (b) clarification needs more info, (c) ambiguity hint surfaces in updated summary

- **Technical dependencies (block the start):** T-E1-US-05-02
- **Delivery dependencies (block the merge):** T-E1-US-05-04, T-E1-US-05-07

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:** Handler at `src/interfaces/workers/message.worker.ts:701` (`handleClarification`). The enriched message is `"${originalMessage} ${userResponse}"`. The state payload `{ missingField, partialExtracted, rawMessage }` is read from the conversation state. Tests already exist in `message.worker.spec.ts:400` for the happy path and "still needs more info" scenarios.
