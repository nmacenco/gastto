### Task T-E1-US-05-06: Add interruption and reformulation user-facing copies

- **Description:** Add new user-facing message copy functions in `expense.copies.ts` for the two new clarification sub-flows. The first is a notification when a previous clarification flow is interrupted by a new expense: "El registro anterior fue cancelado. Procesando nuevo gasto…" (or English equivalent in the project's style). The second is a reformulated clarification question with dynamic options based on the user's context, e.g., "¿El gasto fue en [currency1], [currency2] o [currency3]?".

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] `expenseCopies.clarificationInterrupted()` returns a message notifying the user that the previous registration was cancelled and a new one is being processed
  - [ ] `expenseCopies.clarificationReformulation(options: string[])` returns a message with concrete options based on the provided list
  - [ ] Both functions are unit tested
  - [ ] Messages follow the project's tone and style (Spanish, concise, informative)

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-E1-US-05-04, T-E1-US-05-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** File: `src/application/copies/expense.copies.ts`. Existing copies use Spanish (e.g., `clarificationAmount: () => '¿Cuánto gastaste?'`). Match the tone. Import the new copies in `message.worker.ts` where they are used.
