### Task T-E1-US-01-03: Add expense guidance response use case and copies

- **Description:** Create a use case (or extend `HandleUnsupportedMessage`) that sends a friendly guidance message when a non-financial text is received. The message should teach the user how to register an expense (e.g., "Send me a message with the amount and what you spent it on, like 'Lunch 12 euros'"). The guidance copy must be stored in the copies module following the existing pattern (`src/application/copies/shared.copies.ts`). The use case sends the message exclusively through `MessagingOutputPort` — no direct channel dependency.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] Guidance copy is added to `src/application/copies/shared.copies.ts` (or a dedicated copies file).
  - [x] Copy is friendly, actionable, and in Spanish (consistent with existing copies like `processingAcknowledgment`).
  - [x] Use case sends guidance via `MessagingOutputPort` — no Telegram/WhatsApp imports.
  - [x] Tests verify the guidance message is sent to the correct chat ID.
  - [x] Unit tests pass with `pnpm test`.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-E1-US-01-04

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:**
  - Existing copies: `src/application/copies/shared.copies.ts` (Spanish copies), `src/application/copies/expense.copies.ts`.
  - `MessagingOutputPort` interface: `src/application/ports/output/messaging.port.ts`.
  - If extending `HandleUnsupportedMessage`, update its constructor/execute signature to accept a reason or copy selector.
