### Task T-E1-US-01-04: Update RouteIncomingMessage to classify and route

- **Description:** Modify `RouteIncomingMessage.execute()` to use the `ClassifyFreeTextExpenseIntent` service for TEXT messages. The routing logic becomes:

  - `expense-like` and `too-long` intents: resolve identity, enqueue a `process-message` job, send acknowledgment (existing behavior).
  - `non-financial` intent: send guidance message via `MessagingOutputPort` and do **not** enqueue a job.

  Update `RouteIncomingMessageDeps` to accept the classifier and the guidance handler. Existing behavior for `UNSUPPORTED` and missing-text edge cases must be preserved.

  **Clean Architecture boundary:** The Fastify route handler (`src/interfaces/http/routes/`) only deserializes the raw Telegram webhook body and delegates to this use case. All routing business logic lives here. `MessagingOutputPort` keeps the use case channel-agnostic — no Telegram-specific types leak into the Application layer.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] Expense-like text messages: identity resolved → `process-message` job enqueued → acknowledgment sent.
  - [ ] Non-financial text messages: guidance sent → no job enqueued.
  - [ ] Long messages (>500 chars): treated as `too-long` → enqueued for interpretation.
  - [ ] UNSUPPORTED (non-text) messages: delegated to `HandleUnsupportedMessage` (unchanged).
  - [ ] TEXT messages without text: delegated to `HandleUnsupportedMessage` (unchanged).
  - [ ] All existing unit tests pass; new tests cover classification-based routing.

- **Technical dependencies (block the start):** T-E1-US-01-02, T-E1-US-01-03
- **Delivery dependencies (block the merge):** T-E1-US-01-05

- **Estimation:** 2.0 hours

- **Suggested role:** Backend

- **Technical notes:**
  - File: `src/application/use-cases/conversation/RouteIncomingMessage.ts`
  - Tests: `src/application/use-cases/conversation/RouteIncomingMessage.spec.ts`
  - Run tests with `pnpm test`.
  - The existing acknowledgment (`processingAcknowledgment`) is fire-and-forget — keep that pattern for expense-like messages.
