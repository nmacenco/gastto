### Task T-E1-US-01-05: Add integration tests for free-text expense entry

- **Description:** Write integration tests that exercise the Fastify Telegram webhook route (`POST /webhook/telegram`) for all four acceptance scenarios defined in E1-US-01. Mock the Telegram Bot API HTTP responses, BullMQ queue, and identity resolution. Assert on: HTTP 200 status, job enqueueing for expense-like/too-long messages, guidance message sent for non-financial messages, and no job added for non-financial messages.

- **Architectural layer:** `Cross-cutting` (Interfaces-layer tests)

- **Acceptance criteria:**
  - [x] Happy path test: expense-like text ("Pagué el almuerzo, 12 euros") → 200 OK → queue receives job → ack sent to Telegram.
  - [x] Partial info test: message without currency ("Almuerzo 12") → 200 OK → queue receives job.
  - [x] Non-financial test: "Hola" → 200 OK → guidance sent to Telegram → no queue job.
  - [x] Very long message test: >500 chars → 200 OK → queue receives job.
  - [x] All tests pass with `pnpm test`.

- **Technical dependencies (block the start):** T-E1-US-01-04
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:**
  - Test file location: `src/interfaces/http/routes/` (follow existing pattern).
  - Mock Telegram API using `nock` or MSW — check existing test infrastructure for conventions.
  - Mock BullMQ `Queue.add` to assert on job payload.
  - Mock `MessagingOutputPort.sendMessage` to assert on guidance acknowledgment messages.
  - Run tests with `pnpm test`.
