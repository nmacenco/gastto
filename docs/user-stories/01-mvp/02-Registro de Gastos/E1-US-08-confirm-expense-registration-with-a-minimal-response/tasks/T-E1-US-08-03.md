### Task T-E1-US-08-03: Wire minimal replies into the expense review worker

- **Description:** Update `src/interfaces/workers/message.worker.ts` and the dependency-registration path so `EXPENSE_REVIEW` text messages are delegated once to `ResolveExpenseReviewReplyUseCase`. Preserve callback actions and the existing E1-US-07 `EXPENSE_CORRECTING` entry point, while removing business-rule branching from the worker where the new use case takes responsibility. Keep payload validation, state deserialization, and provider-neutral output delegation at the Interfaces boundary.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [ ] A standard or regional affirmative text received in `EXPENSE_REVIEW` reaches the application use case and triggers the save action without an extra confirmation message.
  - [ ] `comida sí, pero el monto no` reaches the correction flow and does not invoke `RegisterExpenseUseCase.save`.
  - [ ] An uninterpretable response sends the orientation copy and does not transition or modify the pending expense.
  - [ ] Callback-based Confirmar / Corregir / Cancelar behavior remains unchanged.
  - [ ] Dependency construction and worker tests verify the new use case is injected and invoked once per review reply.
  - [ ] The worker contains no new confirmation, correction, or persistence business logic: it only deserializes, validates, and delegates.

- **Technical dependencies (block the start):** T-E1-US-08-02
- **Delivery dependencies (block the merge):** T-E1-US-08-04, T-E1-US-08-05

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the existing thick-worker and dependency-bootstrap patterns in `message.worker.ts` and `src/bootstrap/buildDependencies.ts`. The worker is an Interfaces-layer adapter; the Application use case must remain agnostic of Telegram and WhatsApp and must use an output port for messaging side effects.
