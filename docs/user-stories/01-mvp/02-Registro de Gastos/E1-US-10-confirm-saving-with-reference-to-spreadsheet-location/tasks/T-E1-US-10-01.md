### Task T-E1-US-10-01: Define save-location result contract

- **Description:** Define or extend the Application output-port success result for persisting an expense so it returns the destination sheet name and an optional row number. Model a confirmed save separately from failures so downstream logic cannot produce a success confirmation from an unconfirmed write.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] The spreadsheet-writing output port returns a typed success result containing `sheetName` and an optional `rowNumber`.
  - [ ] A result without a row number remains a valid successful save when a sheet name is available.
  - [ ] Failure results cannot be treated as successful save-location metadata.

- **Technical dependencies (block the start):** E4 spreadsheet-writing contract
- **Delivery dependencies (block the merge):** T-E1-US-10-02, T-E1-US-10-03

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Keep this as an Application port/DTO contract so use cases remain independent of the spreadsheet provider. Apply the project's exact optional-property TypeScript conventions.
