### Task T-E1-US-10-02: Return location metadata from the spreadsheet adapter

- **Description:** Update the spreadsheet-writing infrastructure adapter to map the provider's successful append/write response to the Application save-location result, preserving the actual target sheet and row when the provider exposes it. Return a successful result with no row number only when the provider confirms the write but does not expose that number.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [ ] A confirmed single-sheet write returns the sheet name and appended row number.
  - [ ] A confirmed write to a selected sheet in a multi-sheet spreadsheet returns that selected sheet name and row number.
  - [ ] Confirmed writes without an available row number return the sheet name and no row number.
  - [ ] Provider failures are propagated as failures and do not return success metadata.

- **Technical dependencies (block the start):** T-E1-US-10-01
- **Delivery dependencies (block the merge):** T-E1-US-10-03, T-E1-US-10-05

- **Estimation:** 2.5 hours

- **Suggested role:** Backend

- **Technical notes:** This implements the E4 dependency at the external-service boundary. Mock the spreadsheet provider only in adapter tests; do not infer a row number that was not confirmed by the provider.
