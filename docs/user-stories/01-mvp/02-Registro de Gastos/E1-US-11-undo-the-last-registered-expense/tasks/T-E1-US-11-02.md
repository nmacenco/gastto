### Task T-E1-US-11-02: Persist the latest undoable expense state

- **Description:** Extend the Drizzle expense-record and operation-log repositories so the Application use case can retrieve the user's latest non-deleted expense deterministically, mark that record deleted only after external deletion succeeds, and write the `EXPENSE_DELETED` audit entry with the user, record reference, and timestamp. Preserve the historical record through the existing soft-delete model.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] The latest-record query returns only the most recently saved non-deleted expense for the requested user.
  - [x] A soft-deleted record is excluded from subsequent latest-record queries while its audit history remains available.
  - [x] The persistence operation records the `EXPENSE_DELETED` audit event with the affected record and user context.
  - [x] No local record is marked deleted when the spreadsheet deletion fails.

- **Technical dependencies (block the start):** T-E1-US-11-01; E1-US-10 persisted sheet and row reference
- **Delivery dependencies (block the merge):** T-E1-US-11-04, T-E1-US-11-06

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Reuse the `expense_records` soft-delete and `operation_logs` schema documented in `docs/architecture/data-model.md`; do not create destructive database operations. Use a transaction for the successful local record update plus audit insertion.
