### Task T-E1-US-11-05: Route undo commands and messages

- **Description:** Extend the chat-facing worker or webhook command routing to recognize normalized `deshacer`, `undo`, and `borrar el último` commands, route explicit confirmation replies to the undo use case, and render its typed outcomes as the approved user-facing messages. The handler must not decide which record to delete or access spreadsheet adapters directly.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] The three supported undo commands reach the Application undo use case after input normalization.
  - [x] The immediate-success message identifies the deleted concept and amount.
  - [x] The delayed-flow message identifies the latest concept, amount, and saved time and asks for explicit confirmation before deletion.
  - [x] The no-record and deletion-failure messages are useful, contain no provider internals, and do not imply success.
  - [x] The handler only deserializes, validates, and delegates; it contains no spreadsheet or undo business logic.

- **Technical dependencies (block the start):** T-E1-US-11-04
- **Delivery dependencies (block the merge):** T-E1-US-11-06

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Implement chat delivery as the adapter for the Application output port. Keep command recognition compatible with the existing global cancellation and expense-flow routing.
