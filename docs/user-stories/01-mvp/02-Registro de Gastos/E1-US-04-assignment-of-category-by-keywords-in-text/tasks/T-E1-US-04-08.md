### Task T-E1-US-04-08: Add unit tests for category classification scenarios

- **Description:** Write meaningful unit tests for the keyword-based category classifier covering the Gherkin scenarios: high-confidence assignment, ambiguous keywords, no match, and inferred category not present in the user's spreadsheet. Mock only external boundaries (vocabulary repository), never core business logic.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] Tests cover the unambiguous keyword scenario and assert the correct category and high confidence.
  - [ ] Tests cover the ambiguous keyword scenario and assert the ambiguity flag and user-correction hint path.
  - [ ] Tests cover the no-match scenario and assert the "no category" state.
  - [ ] Tests cover the inferred-category-not-in-spreadsheet scenario and assert the fallback flag.
  - [ ] Tests mock the vocabulary repository and fallback mapper at the boundary, not the classifier internals.
  - [ ] All tests pass with `pnpm test` and respect the Vitest conventions in `AGENTS.md`.

- **Technical dependencies (block the start):** T-E1-US-04-07
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Use Vitest. Place tests alongside the source files or under `tests/` following the project convention. Ensure tests do not require a running database or external spreadsheet. Run `pnpm test` and `pnpm typecheck` before marking done.
