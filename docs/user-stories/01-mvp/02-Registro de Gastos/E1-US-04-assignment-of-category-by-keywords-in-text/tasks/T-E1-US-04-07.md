### Task T-E1-US-04-07: Integrate classifier into conversation expense flow

- **Description:** Wire the category classifier into the conversational expense registration flow after the amount has been detected (E1-US-03). The route handler (Telegram/WhatsApp) must only deserialize the inbound message, validate it, and delegate to the application use case. The use case must remain agnostic of the external messaging service.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] The conversation handler invokes the classification use case after amount detection.
  - [x] The route/handler layer only deserializes, validates, and delegates; it contains no category business logic.
  - [x] The classification result is added to the expense DTO/state used to build the confirmation summary.
  - [x] Ambiguous, fallback, and "no category" states are surfaced in the summary with a prompt for correction.
  - [x] The flow is not blocked when confidence is low or no category is found.
  - [x] Zod schemas are updated if the expense interpretation DTO changes.

- **Technical dependencies (block the start):** T-E1-US-04-04, T-E1-US-04-06
- **Delivery dependencies (block the merge):** T-E1-US-04-08

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the Clean Architecture boundary guardrail: handler (Interfaces) → use case (Application) → output port (Application) → adapter (Infrastructure). Refer to existing Telegram/WhatsApp route handlers in `src/interfaces/` and the OpenAPI/Zod rules in `AGENTS.md`.
