### Task T-E1-US-04-03: Define category classifier application ports

- **Description:** Define the application-layer ports (interfaces) that keep the classification use case decoupled from infrastructure. This includes an input port representing the classification service contract and an output port for retrieving the user's confirmed category vocabulary.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] An input port (e.g., `ICategoryClassifier`) exists in `src/application/ports/in/` or equivalent.
  - [ ] An output port (e.g., `ICategoryVocabularyRepository`) exists in `src/application/ports/out/` or equivalent.
  - [ ] Port signatures accept domain value objects and plain primitives, not framework-specific types.
  - [ ] Ports are imported by the use case but implemented only by infrastructure adapters.
  - [ ] No HTTP, database, or messaging types leak into the port definitions.

- **Technical dependencies (block the start):** T-E1-US-04-02
- **Delivery dependencies (block the merge):** T-E1-US-04-04, T-E1-US-04-05, T-E1-US-04-07

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Align the port file structure with the existing `src/domain/ports/` convention observed in `AGENTS.md`. Keep the vocabulary retrieval port minimal: `findByUserId(userId: string): Promise<CategoryKeywordVocabulary>`.
