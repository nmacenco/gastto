### Task T-E1-US-03-06: Wire extraction into Telegram message processing pipeline

- **Description:** Integrate the amount/currency extraction use case into the existing message processing pipeline from E1-US-01/E1-US-02. In the Telegram route handler (`src/interfaces/http/routes/` or the Telegram adapter in `src/infrastructure/adapters/telegram/`), deserialize and validate the incoming webhook payload, then delegate to the application use case. Implement the output port to send clarification messages back to the user through the Telegram adapter. The handler must not contain business rules.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [ ] Telegram webhook route validates incoming payload and delegates to the application use case.
  - [ ] Successful extraction transitions the conversation to the next step.
  - [ ] Missing/ambiguous/zero amount triggers a clarification message sent via Telegram.
  - [ ] No business logic about amounts, currencies, or clarifications leaks into the route handler.

- **Technical dependencies (block the start):** T-E1-US-03-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** Backend

- **Technical notes:** Register the route with Zod schemas for `@fastify/swagger` per `AGENTS.md`. If the route is already registered by E1-US-01, extend it by injecting the new use case. The handler's responsibility is limited to: parse request → validate → call use case → return/forward response.
