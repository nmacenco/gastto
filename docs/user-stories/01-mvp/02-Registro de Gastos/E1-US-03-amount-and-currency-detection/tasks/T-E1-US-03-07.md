### Task T-E1-US-03-07: Write unit tests for amount and currency extraction

- **Description:** Write comprehensive Vitest unit tests for the extraction service implemented in T-E1-US-03-03, covering all documented variants: symbols before numbers, ISO codes after numbers, thousands and decimal separators with period or comma, ambiguous symbols, missing amounts, and zero amounts. Aim for ≥ 90% coverage of the extraction function. Mock only external boundaries (e.g., repository), not the extraction logic itself.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [x] Tests cover all acceptance scenarios from the User Story.
  - [x] Coverage of the extraction function is ≥ 90%.
  - [x] Ambiguous currency cases are tested with and without a default currency.
  - [x] Zero amount and missing amount cases are tested.
  - [x] `pnpm test` passes.

- **Technical dependencies (block the start):** T-E1-US-03-03
- **Delivery dependencies (block the merge):** None

- **Estimation:** 3 hours

- **Suggested role:** Backend / QA

- **Technical notes:** Place tests in `tests/unit/` mirroring the service path. Use Vitest's `describe`/`it` blocks. Run `pnpm test:coverage` to verify the threshold. Do not mock the parsing internals; test the public API of the extraction service.
