### Task T-E1-US-09-04: Wire the message boundary to the cancellation use case

- **Description:** Connect the existing inbound message route or webhook adapter to the cancellation-aware application use case. Ensure incoming payloads are deserialized and validated at the Interfaces boundary, then delegated without embedding cancellation rules or provider-specific business logic in the route.

- **Architectural layer:** `Interfaces`

- **Acceptance criteria:**
  - [x] The inbound route passes the normalized message data to the Application use case that owns cancellation behavior.
  - [x] The route handler only deserializes, validates, and delegates; it does not decide whether a message cancels a flow.
  - [x] Any new or changed Fastify route declares Zod schemas with tags, description, and response schemas for OpenAPI generation.
  - [x] The use case communicates responses through the Application output port and remains agnostic of Telegram, WhatsApp, or HTTP details.
  - [x] Route-level tests verify delegation for cancellation messages and non-cancellation expense messages.

- **Technical dependencies (block the start):** T-E1-US-09-03
- **Delivery dependencies (block the merge):** T-E1-US-09-05

- **Estimation:** 1.5 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the existing webhook route patterns under `src/interfaces/http/routes/`. For independently instantiated Fastify tests, register `validatorCompiler` and `serializerCompiler` from `fastify-type-provider-zod`.
