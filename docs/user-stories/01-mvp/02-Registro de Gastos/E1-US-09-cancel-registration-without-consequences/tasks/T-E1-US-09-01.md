### Task T-E1-US-09-01: Define the cancellation contract

- **Description:** Define the application-level cancellation command set, result states, user-facing confirmation messages, and output port needed to cancel an active expense-registration flow or report that no flow is active. Keep the contract independent from Telegram, WhatsApp, or any other external transport.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [x] The application contract represents cancellation independently of the external messaging provider.
  - [x] Supported cancellation inputs are defined for `no`, `cancel`, `cancel it`, `do not register`, `stop`, and `exit` after normalization.
  - [x] The contract distinguishes an active-flow cancellation from a cancellation request when no flow is active.
  - [x] Unit tests cover the contract's supported commands and result states without mocking core business logic.
  - [x] The route handler boundary is explicit: the Interfaces layer only deserializes, validates, and delegates; the Application use case contains all business logic; an Application output port keeps the use case agnostic of the external service.

- **Technical dependencies (block the start):** E1-US-05, E1-US-06, E1-US-07
- **Delivery dependencies (block the merge):** T-E1-US-09-02, T-E1-US-09-03

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** Follow the existing use-case, DTO, and port conventions under `src/application/` and `src/domain/ports/`. Keep user-facing text centralized in the application output contract. Update the relevant feature documentation when implementation begins.
