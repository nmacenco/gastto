### Task T-E1-US-09-05: Verify cancellation rollback with integration tests

- **Description:** Add an integration test covering cancellation from active expense-flow states through the inbound message boundary and persistence adapters. Verify that cleanup is complete, the expected response is emitted, no orphaned data remains, and a new expense can start immediately.

- **Architectural layer:** `Cross-cutting`

- **Acceptance criteria:**
  - [ ] The integration test covers cancellation during clarification, summary, and correction, including the global `stop` command.
  - [ ] The test verifies the explicit confirmation response and the friendly no-active-flow response.
  - [ ] The test asserts that conversational state and staged expense data are absent after cancellation.
  - [ ] The test sends a new expense immediately afterward and verifies it starts with a clean context.
  - [ ] The test fails if cancellation leaves orphaned data or causes the next expense to inherit the canceled context.

- **Technical dependencies (block the start):** T-E1-US-09-02, T-E1-US-09-03, T-E1-US-09-04
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2.5 hours

- **Suggested role:** QA / Backend

- **Technical notes:** Use Vitest and mock only external boundaries such as network, filesystem, or database connections. Follow `docs/testing/guidelines.md`; run `pnpm test` and include meaningful state/persistence assertions rather than only response snapshots.
