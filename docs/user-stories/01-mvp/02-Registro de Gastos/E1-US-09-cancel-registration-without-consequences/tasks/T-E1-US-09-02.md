### Task T-E1-US-09-02: Implement complete conversation-context cleanup

- **Description:** Implement the state cleanup operation used when an expense-registration flow is canceled. Remove the active conversational context and any staged or in-progress expense data from the configured persistence mechanisms, while preserving unrelated user data and ensuring the operation is safe when no context exists.

- **Architectural layer:** `Infrastructure`

- **Acceptance criteria:**
  - [x] The cleanup operation removes all active-flow state for the user, including clarification, summary, and correction data.
  - [x] Any staged expense data managed by the persistence adapter is removed or marked as non-persisted before cancellation completes.
  - [x] Cleanup is idempotent and succeeds when no active context exists.
  - [x] Existing data belonging to other users or completed registrations is not modified.
  - [x] Adapter-level tests verify that no orphaned active-flow records remain after cleanup.

- **Technical dependencies (block the start):** T-E1-US-09-01
- **Delivery dependencies (block the merge):** T-E1-US-09-03

- **Estimation:** 2.5 hours

- **Suggested role:** Backend

- **Technical notes:** Inspect the existing conversational-state adapter under `src/infrastructure/redis/` and any related repositories under `src/infrastructure/db/repositories/`. Respect the project's transaction and foreign-key conventions if more than one persistence store is involved. Do not remove completed expense records.
