### Task T-E1-US-04-05: Add unit tests for all classification scenarios

- **Description:** Write comprehensive unit tests for the keyword classification system. Cover all five Gherkin scenarios from the user story plus edge cases. Test at the classifier service level (unit, no IO) and at the use case level (with mocked `CategoryVocabularyPort`). Include tests for: unambiguous keyword match, multiple keywords same category, ambiguous keywords across categories, no keywords detected, and inferred category not in user's spreadsheet. Also test confidence boundary conditions and the non-blocking behavior when confidence is low/none.

- **Architectural layer:** `Cross-cutting` (testing)

- **Acceptance criteria:**
  - [ ] Test: unambiguous keyword present returns correct category with `high` confidence.
  - [ ] Test: multiple keywords all pointing to same category return that category with `high` confidence.
  - [ ] Test: ambiguous keywords return `medium`/`low` confidence with alternatives listed.
  - [ ] Test: no keywords detected return `none` confidence and no category.
  - [ ] Test: inferred category not in user's spreadsheet maps to closest match or uncategorized.
  - [ ] Test: the use case does not block confirmation when category confidence is `low`/`none`.
  - [ ] All tests pass with `pnpm test`.

- **Technical dependencies (block the start):** T-E1-US-04-04
- **Delivery dependencies (block the merge):** None

- **Estimation:** 2.5 hours

- **Suggested role:** Backend / QA

- **Technical notes:**
  - Test file location: alongside the classifier and use case (e.g., `src/application/use-cases/expense/__tests__/`).
  - Use Vitest (`pnpm test`).
  - Mock `CategoryVocabularyPort` to test the spreadsheet mapping without real DB.
  - Test Spanish keywords as used in the Gherkin scenarios.
