### Task T-E1-US-06-01: Define expense summary output port and DTO

- **Description:** Create the `ExpenseSummary` DTO in `src/application/dtos/` containing fields: concept, amount, currency, category, date, and confidence flags per field. Define the output port interface (`ExpenseSummaryPresenter`) in `src/application/ports/` that keeps the use case agnostic of the messaging platform (Telegram, WhatsApp). The presenter contract must expose methods for presenting the summary, showing a warning, requesting confirmation, and notifying cancellation.

- **Architectural layer:** `Application`

- **Acceptance criteria:**
  - [ ] `ExpenseSummary` DTO exists with typed fields: concept, amount, currency, category, date, and per-field confidence level.
  - [ ] `ExpenseSummaryPresenter` interface is defined in the application ports directory.
  - [ ] Presenter methods: `presentSummary(summary)`, `showTimeoutWarning()`, `notifyCancellation()`, `requestHighAmountConfirmation()`.
  - [ ] All types are exported and importable by infrastructure adapters.

- **Technical dependencies (block the start):** None
- **Delivery dependencies (block the merge):** T-E1-US-06-02, T-E1-US-06-05

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** Follow the existing port/interface pattern in `src/application/ports/`. The DTO should not contain any formatting logic — it is pure data. The presenter interface should mirror the messaging platform's capabilities (text, markdown, inline buttons).
