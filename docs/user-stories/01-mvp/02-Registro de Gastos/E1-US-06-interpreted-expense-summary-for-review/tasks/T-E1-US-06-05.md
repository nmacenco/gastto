### Task T-E1-US-06-05: Wire summary presentation into Telegram message handler

- **Description:** Implement the Telegram-side presenter adapter (`TelegramExpenseSummaryPresenter`) in `src/infrastructure/adapters/telegram/` that implements the `ExpenseSummaryPresenter` interface. Format the summary as a Telegram message with: clear field labels, markdown formatting, low-confidence markers (❓), high-amount warning (⚠️), and inline action buttons (Confirm / Correct / Cancel). Register the presenter in the DI container. Update the Telegram message handler to invoke `GenerateExpenseSummaryUseCase` after expense interpretation is complete.

- **Architectural layer:** `Interfaces` / `Infrastructure`

- **Acceptance criteria:**
  - [x] `TelegramExpenseSummaryPresenter` implements `ExpenseSummaryPresenter`.
  - [x] Summary message includes all five fields with proper formatting.
  - [x] Low-confidence category is marked with ❓ or "(Correct?)".
  - [x] High-amount warning shows ⚠️ and requires explicit confirmation.
  - [x] Inline keyboard includes Confirm, Correct, Cancel buttons.
  - [x] Route handler calls the use case and does not contain business logic.
  - [x] WhatsApp compatibility is considered but not required for this task.

- **Technical dependencies (block the start):** T-E1-US-06-01, T-E1-US-06-02, T-E1-US-06-03, T-E1-US-06-04
- **Delivery dependencies (block the merge):** T-E1-US-06-06, T-E1-US-06-07

- **Estimation:** 2 hours

- **Suggested role:** Backend

- **Technical notes:** See existing Telegram adapters in `src/infrastructure/adapters/telegram/` for pattern reference. Use `telegraf` inline keyboard markup. The route handler at `src/interfaces/http/routes/webhook/telegram.ts` is where the handler logic lives.
