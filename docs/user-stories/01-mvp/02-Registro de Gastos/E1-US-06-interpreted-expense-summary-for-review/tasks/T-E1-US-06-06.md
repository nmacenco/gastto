### Task T-E1-US-06-06: Implement confirm/correct/cancel action resolution

- **Description:** Implement the action resolution flow for user responses to the expense summary. When the user clicks "Confirm", trigger the save expense use case. When the user clicks "Correct", enter a sub-dialog asking which field to change and the new value. When the user clicks "Cancel", discard the interpreted expense and clear the state. All action routing logic belongs in the Application layer use case — the route handler only deserializes the callback data and delegates.

- **Architectural layer:** `Application` / `Interfaces`

- **Acceptance criteria:**
  - [x] "Confirm" action triggers the expense save flow.
  - [x] "Correct" action enters field-correction sub-dialog (reuses E1-US-07 patterns).
  - [x] "Cancel" action discards the expense and cleans up the state.
  - [x] Action handlers are wired to Telegram callback query handlers.
  - [x] The route handler only deserializes callback data and delegates to the use case.

- **Technical dependencies (block the start):** T-E1-US-06-05
- **Delivery dependencies (block the merge):** None

- **Estimation:** 1 hour

- **Suggested role:** Backend

- **Technical notes:** The correct action is a stub that delegates to E1-US-07's correction flow. Confirm should invoke the save expense use case (E1-US-10). Cancel should invoke the cancellation use case (E1-US-09). Follow the existing callback query pattern in the Telegram handler.
