# Runtime
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database — Supabase development project
# Example: postgresql://postgres:password@db.project.supabase.co:5432/postgres
DATABASE_URL=

# Cache / Queue — Redis local (Docker)
# Example: redis://localhost:6379
REDIS_URL=

# Category classification (E1-US-04)
# Minimum confidence for keyword-based category classification. Range: [0, 1]. Default: 0.6.
CATEGORY_CLASSIFICATION_CONFIDENCE_THRESHOLD=0.6

# LLM
# At least one provider is required.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
NVIDIA_API_KEY=

# Messaging
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_BOT_TOKEN=
# Base URL where the Telegram webhook is reachable (e.g. https://gastto-develop.fly.dev).
# Used to auto-register the webhook with Telegram Bot API on startup.
# For local development use http://localhost:3000 (auto-registration is skipped for localhost).
WEBHOOK_BASE_URL=http://localhost:3000

# Observability
SENTRY_DSN=

# OAuth
# Must match the registered callback route exactly: /auth/google/callback
# Update this in your Google Cloud Console > APIs & Credentials > Authorized redirect URIs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Security
# AES-256-GCM key for OAuth token encryption (ADR-007). Must be 32 bytes (64 hex chars).
ENCRYPTION_KEY=
